---
description: E02 workspace guard — agent must stay in assigned run folder
globs: "**/*"
alwaysApply: true
---

# E02 workspace guard

You are running dogfood experiment **E02**. Your workspace root must be exactly one of:

- `.../ab-p25-gap-pass-validation/runs/B0` (baseline)
- `.../ab-p25-gap-pass-validation/runs/G1` (Trace arm)

**Turn 1:** Run `pwd` and confirm. If wrong, stop and ask the operator to Open Folder on the correct run directory.

Never edit under the Trace monorepo root or sibling experiment paths.
