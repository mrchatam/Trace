# Incident tracker (starter)

Empty starter for experiment E01. The agent chooses stack and implements the full system.

Do not commit secrets. Tests should pass with `go test ./...` when the agent is done.
