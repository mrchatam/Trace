# S05 — Regression / reflect — scope todos

| Order | ID | Role | Status cue |
|------:|----|------|------------|
| 1 | P20-S05-00 | scope planner | done |
| 2 | P20-S05-01 | implementer | pending |
| 3 | P20-S05-02 | reviewer | done — APPROVE high; causal honesty verified |

**Depends on:** S03 contradicted effects + optional `hypothesis_explains_effect`; S04 evaluation `comparison_json` regression flags; S02 hypotheses (`ConfirmHypothesis`); S01 `PolicyInputs.OpenRegression`. **Feeds:** S06 wires `HasOpenRegression`; S07 correlated ≠ caused fail bar.

**FINAL locks (S05-00):** `regressions` + `reflections`; migration **019**; compat ceiling **19**; attribution `correlated|hypothesized|caused` (never auto-caused); reflections structured JSON arrays (no essay `body`); `observed_relationship` vs `caused_by` (causal requires evidence); library-only; §18 Future stub only.
