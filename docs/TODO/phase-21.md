Index: [`docs/TODO.md`](../TODO.md)

## Phase 21 — TRACE thoughtprocess completion

Human-promoted forward follow-on after Phase 20 close. Completes P20 **residuals**, **gaps** vs [`TRACE_THOUGHTPROCESS.md`](../TRACE_THOUGHTPROCESS.md), and **promoted MVP cuts** deferred by P20 planners (see [`DECISION-LOG.md`](../phases/phase-21-thoughtprocess-completion/DECISION-LOG.md)). Work map: [`WORK-MAP.md`](../phases/phase-21-thoughtprocess-completion/WORK-MAP.md).

| Order | ID | Status | Prompt | Notes |
|------:|----|--------|--------|-------|
| 293 | P21-00 | done | [phases/phase-21-thoughtprocess-completion/00-PHASE-PLANNER.md](../phases/phase-21-thoughtprocess-completion/00-PHASE-PLANNER.md) | 2026-08-18: phase planner **done**. Live inventory: schema max **019**; seed export omits 11 P20 tables; SelectNext **8-row MVP** (EXECUTE never returned); retrieval lacks uncertainty/hypothesis/change/etc.; FTS skip on cognitive upserts; compat ceiling **19**. WORK-MAP confirmed W-01…W-15; mig locks S01 none / S04 **020** / S07 **021**. Thickened S01–S08 all `00-PLANNER` + `01-*` + `02-*` with named tests + touch files. DECISION-LOG unchanged. DR-HANDOFF **OPEN**. No product Go. **Next `P21-S01-00`**. |
| 294 | P21-S01-00 | done | [phases/phase-21-thoughtprocess-completion/scopes/scope-01-portable-p20-seed/00-PLANNER.md](../phases/phase-21-thoughtprocess-completion/scopes/scope-01-portable-p20-seed/00-PLANNER.md) | 2026-08-18: scope planner **done**. Live confirmed: schema max **019** (015–019, no 020); compat ceiling **19**; `SeedDocument` has no P20 fields yet; P17 keepers `TestSeedExportRoundTrip`, `TestSeedExportOmitsDeniedSurfaces`, `TestSeedExportWritesExportedAtCommit` in `cmd/trace/cli_test.go`. Locked: seed v1 additive, **no mig 020**, 11 tables → 10 JSON keys (`change_paths` nested in `changes[].paths`), import order baselines→outcomes→changes/effects→cognitive→regressions/reflections→deliberation_states, `ListAll*` store helpers. Retires P20 verify omit policy (D-05). Thickened `01-portable-p20-seed.md` + `02-scope-review.md`. No product Go. **Next `P21-S01-01`**. |
| 295 | P21-S01-01 | done | [phases/phase-21-thoughtprocess-completion/scopes/scope-01-portable-p20-seed/01-portable-p20-seed.md](../phases/phase-21-thoughtprocess-completion/scopes/scope-01-portable-p20-seed/01-portable-p20-seed.md) | 2026-08-18: **P20 seed export/import landed** — 10 JSON keys (11 tables; `change_paths` nested in `changes[].paths`); seed v1 additive; no mig 020; compat ceiling 19 unchanged. Added `ListAll*` store helpers + idempotent import order baselines→outcomes→changes/effects→cognitive→regressions/reflections→deliberation_states. **Tests PASS:** `go test ./internal/domain/... -count=1 -run 'TestSeedExportIncludesP20Cognition|TestSeedImportP20RoundTrip'`; `CGO_ENABLED=1 go test ./cmd/trace/... -count=1 -run 'TestSeedExportRoundTrip|TestSeedExportOmitsDeniedSurfaces|TestSeedExportWritesExportedAtCommit|TestLoopApplyDeliberationTransitionEvent'`; `go test ./evals/compat/... -count=1`. Exported `trace/graph.json` (P20 keys omitted — local `.trace/` has no P20 rows; keys appear when DB populated per tests). **Next P21-S01-02**. |
| 296 | P21-S01-02 | done | [phases/phase-21-thoughtprocess-completion/scopes/scope-01-portable-p20-seed/02-scope-review.md](../phases/phase-21-thoughtprocess-completion/scopes/scope-01-portable-p20-seed/02-scope-review.md) | 2026-08-18: **review APPROVE — confidence high**. Claimed deliverables match repo: `SeedDocument` has 10 P20 JSON keys (11 tables; `change_paths` nested in `changes[].paths`); `BuildSeedDocument`/`ImportSeedDocument` cover all 11 tables; FK-safe import order baselines→outcomes→changes/effects→cognitive→regressions/reflections→deliberation_states; 10 `ListAll*` helpers only (schema max **019**, no **020+**); compat ceiling **19** unchanged. D-05 omit policy **retired** — portable P20 cognition round-trips. Denied surfaces still omitted (transitions/work_state/reviews/caps/tokens/index). **Tests PASS (reviewer re-run):** `go test ./internal/domain/... -count=1 -run 'TestSeedExportIncludesP20Cognition|TestSeedImportP20RoundTrip'` → ok 0.144s; `CGO_ENABLED=1 go test ./cmd/trace/... -count=1 -run 'TestSeedExportRoundTrip|TestSeedExportOmitsDeniedSurfaces|TestSeedExportWritesExportedAtCommit'` → ok 0.201s; `go test ./evals/compat/... -count=1 -run TestCompatibilitySecurityChecklist` → ok 0.891s. `trace/graph.json` present (131 lines); P20 keys absent — expected (local `.trace/` has no P20 rows; unit tests prove keys when populated). **Findings:** 0 blocker/high; 1 low (no dedicated old-seed-without-P20-keys test — mitigated by `TestSeedExportRoundTrip` P17-only import + empty-slice no-op loops). No spawns. **Next P21-S02-00**. |
| 297 | P21-S02-00 | done | [phases/phase-21-thoughtprocess-completion/scopes/scope-02-retrieval-fts/00-PLANNER.md](../phases/phase-21-thoughtprocess-completion/scopes/scope-02-retrieval-fts/00-PLANNER.md) | 2026-08-18: scope planner **done**. Live reconfirmed: schema max **019**; compat **19**; `lookupEntity` ends at symbol (no P20); `NormalizeEntityType` only `plan-change` alias; `loadEntityText`/`RebuildFTS` pre-P20 tables; `UpsertUncertainty`/`UpsertHypothesis` skip FTS (comments L95/L165); domain has 8 P20 entity consts; S01 seed landed — **no seed changes** for S02. INVESTIGATE stderr root cause locked: `TaskContext`→`Expand`→`uncertainty_blocks_task` link→unknown type→`minimalTaskContextPacket` fallback. FINAL locks unchanged: 8 canonical types, FTS Must/Should split, fail-closed, no mig 020. Thickened `01-retrieval-fts.md` + `02-scope-review.md` with root-cause chain, entity mappings, 8 named tests, touch files, keeper floor. No product Go. **Next `P21-S02-01`**. |
| 298 | P21-S02-01 | done | [phases/phase-21-thoughtprocess-completion/scopes/scope-02-retrieval-fts/01-retrieval-fts.md](../phases/phase-21-thoughtprocess-completion/scopes/scope-02-retrieval-fts/01-retrieval-fts.md) | 2026-08-18: **P20 retrieval + FTS landed**. `lookupEntity` + `NormalizeEntityType` register 8 P20 types (`outcome`→`outcome_result` alias); FTS Must/Should tables in `loadEntityText`/`RebuildFTS`/`ensureFTSPopulated`; `UpsertUncertainty`/`UpsertHypothesis` sync FTS; INVESTIGATE stderr fixed (Expand resolves uncertainty neighbors). No mig 020; compat ceiling 19 unchanged; no seed edits. **Tests PASS:** `go test ./internal/retrieval/... -count=1 -run 'TestExactLookupUncertainty|TestExactLookupHypothesis|TestWhyUncertaintyIncludesGraphSteps|TestNormalizeEntityTypeP20Aliases'` → ok; `go test ./internal/store/... -count=1 -run 'TestSyncEntityFTSUncertainty|TestRebuildFTSIncludesP20Types'` → ok; `CGO_ENABLED=1 go test ./cmd/trace -count=1 -run 'TestLoopNextInvestigateNoRetrievalStderr|TestCausalWhyContextRoundTrip|TestLoopNextInvestigateEmphasizesUncertainties'` → ok; `go test ./internal/deliberation/... -count=1` → ok; `go test ./evals/compat/... -count=1 -run TestCompatibilitySecurityChecklist` → ok. **Next P21-S02-02**. |
| 299 | P21-S02-02 | done | [phases/phase-21-thoughtprocess-completion/scopes/scope-02-retrieval-fts/02-scope-review.md](../phases/phase-21-thoughtprocess-completion/scopes/scope-02-retrieval-fts/02-scope-review.md) | 2026-08-18: **review APPROVE — confidence high**. Claimed S02-01 deliverables match repo: `lookupEntity` registers all 8 P20 types (`exact.go` L208–314); `NormalizeEntityType` adds `outcome`→`outcome_result` + `plan-change`→`plan_change` pass-through (`types.go` L74–79); unknown type fail-closed at `lookupEntity` default (`exact.go` L316) + `loadEntityText` default (`fts.go` L426); FTS Must+Should in `RebuildFTS`/`ensureFTSPopulated` (uncertainties…outcome_results; no `effect` rebuild — Exact-only `fts.go` L422–424); `UpsertUncertainty`/`UpsertHypothesis` call `SyncEntityFTS` (comments removed); schema max **019** (19 files, no 020+); compat ceiling **19** unchanged; S01 seed files untouched. **D-06 closed:** `TestLoopNextInvestigateNoRetrievalStderr` — no `retrieval: unknown entity type` on INVESTIGATE stderr. **D-07 closed:** `TestSyncEntityFTSUncertainty` + `TestRebuildFTSIncludesP20Types` — P20 rows indexed after upsert/rebuild. **Tests PASS (reviewer re-run):** retrieval 4/4 → ok 0.075s; store 3/3 → ok 0.077s; cmd/trace 4/4 → ok 0.208s; deliberation → ok 0.002s; `CGO_ENABLED=1` compat → ok 0.893s. **Findings:** 0 blocker/high; 1 low (no dedicated unit test for unknown-type fail-closed at Exact/Why boundary — mitigated by default-case errors + INVESTIGATE negative assertion). No spawns. **Next P21-S03-00**. |
| 300 | P21-S03-00 | done | [phases/phase-21-thoughtprocess-completion/scopes/scope-03-full-phase-cycle/00-PLANNER.md](../phases/phase-21-thoughtprocess-completion/scopes/scope-03-full-phase-cycle/00-PLANNER.md) | 2026-08-18: scope planner **done**. Live confirmed: `select.go` **8-row MVP** (EXECUTE never returned; CRITIQUE at row 6 without EXPLORE); `PolicyInputs` **6 fields**; `PhaseContextProfile` already has EXECUTE/TEST/EVALUATE/REFLECT/REPLAN; `BuildPolicyInputs` stubs cycle flags; schema max **019**; compat **19**. Locked W-04/W-15: **14-row FINAL** table (EXPLORE row 6 when alternatives + !critiqued; rows 8–13 EXECUTE→TEST→VERIFY→EVALUATE→REFLECT→REPLAN); 8 new PolicyInputs fields + optional floats (D-18 observability-only); EXECUTE gate unchanged; no ML; no mig 020. Thickened `01-full-phase-cycle.md` + `02-scope-review.md` with live before-state, 9 named tests, keeper floor. No product Go. **Next `P21-S03-01`**. |
| 301 | P21-S03-01 | done | [phases/phase-21-thoughtprocess-completion/scopes/scope-03-full-phase-cycle/01-full-phase-cycle.md](../phases/phase-21-thoughtprocess-completion/scopes/scope-03-full-phase-cycle/01-full-phase-cycle.md) | 2026-08-18: **14-row SelectNext + PolicyInputs landed**. Extended `PolicyInputs` with 8 cycle fields + optional floats (D-18 observability-only); 6 new reason codes; `select.go` FINAL table (EXPLORE row 6; EXECUTE→TEST→VERIFY→EVALUATE→REFLECT→REPLAN rows 8–13); EXPLORE `PhaseContextProfile` added. `BuildPolicyInputs` stubs new fields (S06 wires live). No mig 020; compat ceiling 19 unchanged. **Tests PASS:** `go test ./internal/deliberation/... -count=1 -run 'TestSelectNext|TestSelectNextNeverExecuteOnBlockingUncertainty|TestSelectNextExecuteWhenPending|TestSelectNextTestWhenExecuteDone|TestSelectNextEvaluateWhenTestPass|TestSelectNextReflectWhenEvaluationDone|TestSelectNextReplanWhenFlagged|TestSelectNextExploreWhenAlternatives|TestSelectNextFullCycleOrdering|TestTransitionPayloadIncludesOptionalScores|TestApplyTransition|TestTransitionPayloadJSONRequiredFields'` → ok; `go test ./cmd/trace -count=1 -run 'TestLoopNextDeliberationSectionPresent|TestLoopNextExecuteEmphasizesContextAndRelated|TestLoopApplyDeliberationTransitionEvent'` → ok; `go test ./internal/domain/... -count=1 -run 'TestApplyDeliberationTransition|TestCountBlockingUncertaintiesFeedsApplyDeliberationTransition|TestHasOpenRegressionFeedsApplyDeliberationTransition'` → ok; `CGO_ENABLED=1 go test ./evals/compat/... -count=1 -run TestCompatibilitySecurityChecklist` → ok. **Next P21-S03-02**. |
| 302 | P21-S03-02 | done | [phases/phase-21-thoughtprocess-completion/scopes/scope-03-full-phase-cycle/02-scope-review.md](../phases/phase-21-thoughtprocess-completion/scopes/scope-03-full-phase-cycle/02-scope-review.md) | 2026-08-18: **review APPROVE — confidence high**. S03-01 deliverables match repo: `select.go` **14-row FINAL** first-match table (EXPLORE row 6; EXECUTE→TEST→VERIFY→EVALUATE→REFLECT→REPLAN rows 8–13); `PolicyInputs` 6 legacy + 8 new fields + optional floats (D-18 observability-only, unused in `SelectNext`); 6 new reason codes; `ApplyTransition` embeds full `policy_inputs`; `select.go`/`deliberation` package pure (no store/domain/planner I/O). **D-03 closed:** EXECUTE/TEST/EVALUATE/REFLECT/REPLAN returned when pending flags set. **D-04 closed:** EXPLORE row 6 only when `open_decision_alternatives > 0` && `!plan_critiqued`. **D-18 closed:** floats in transition JSON only; priority order unchanged. `BuildPolicyInputs` still stubs cycle flags (expected until S06). Schema max **019** (19 files, no **020+**); compat ceiling **19**; `NextSchemaVersion` still `trace.loop.next.v1`. **Tests PASS (reviewer re-run):** `go test ./internal/deliberation/... -count=1` → ok 0.002s; deliberation 9/9 named `-run 'TestSelectNextNeverExecuteOnBlockingUncertainty|TestSelectNextExecuteWhenPending|TestSelectNextTestWhenExecuteDone|TestSelectNextEvaluateWhenTestPass|TestSelectNextReflectWhenEvaluationDone|TestSelectNextReplanWhenFlagged|TestSelectNextExploreWhenAlternatives|TestSelectNextFullCycleOrdering|TestTransitionPayloadIncludesOptionalScores'` + legacy `TestSelectNext`/`TestApplyTransition`/`TestTransitionPayloadJSONRequiredFields` → ok; `go test ./cmd/trace -count=1 -run 'TestLoopNext|TestLoopApplyDeliberationTransitionEvent|TestLoopApplyUncertaintyWriteAffectsNextSelectNext|TestLoopApplyRegressionWriteAffectsPolicyInputs|TestLoopNextDeliberationSectionPresent|TestLoopNextExecuteEmphasizesContextAndRelated'` → ok 0.630s; domain feeders 3/3 → ok 0.120s; `CGO_ENABLED=1 go test ./evals/compat/... -count=1 -run TestCompatibilitySecurityChecklist` → ok 0.898s. P20 keepers green (`TestSelectNextNeverExecuteOnBlockingUncertainty`, sticky INVESTIGATE hop budget, `TestLoopApplyDeliberationTransitionEvent`). **Findings:** 0 blocker/high; 1 low (no dedicated dual-flag subtest `verification_incomplete` + `evaluation_pending` → VERIFY — mitigated by row 10 before 11 in `select.go` + per-row cycle tests). No spawns. **Next P21-S04-00**. |
| 303 | P21-S04-00 | done | [phases/phase-21-thoughtprocess-completion/scopes/scope-04-baseline-promotion/00-PLANNER.md](../phases/phase-21-thoughtprocess-completion/scopes/scope-04-baseline-promotion/00-PLANNER.md) | 2026-08-18: scope planner **done**. Live confirmed: schema max **019** (19 files, no 020); `CreateBaseline` insert-only; `CompareScoresToBaseline` computes `overall_regression`; gate helpers Test/Verification/Evaluation only — no `CheckPromotionGate`; loop `statusBlocked` = uncertainty/regression/debt only; compat ceiling **19**. Locked W-05/W-06: mig **020_baselines_promotion.sql** (`status` active\|superseded + `supersedes_id`); `PromoteBaseline`/`SupersedeBaseline` chain key git_commit+label; `CheckPromotionGate` advisory (`eval_regression` / `no_stored_evaluation`); loop additive `promotion_blocked` — no auto work_state; compat **20** (forbid 021+). 6 named tests locked. Thickened `01-baseline-promotion.md` + `02-scope-review.md`. No product Go. **Next `P21-S04-01`**. |
| 304 | P21-S04-01 | done | [phases/phase-21-thoughtprocess-completion/scopes/scope-04-baseline-promotion/01-baseline-promotion.md](../phases/phase-21-thoughtprocess-completion/scopes/scope-04-baseline-promotion/01-baseline-promotion.md) | 2026-08-18: **baseline promotion + eval regression gate landed**. Mig **020_baselines_promotion.sql** (`status` active\|superseded + `supersedes_id`); `PromoteBaseline`/`SupersedeBaseline` chain key git_commit+label; advisory `CheckPromotionGate` (`eval_regression` / `no_stored_evaluation`); loop `promotion_blocked` on `trace.loop.status.v1`; seed optional `status`/`supersedes_id`; compat ceiling **20** (forbid 021+). **Tests PASS:** `go test ./internal/domain/... -count=1 -run 'TestPromoteBaselineSupersedesPrior|TestPromoteBaselineIdempotent|TestEvalRegressionBlocksPromotionGate|TestEvalRegressionGateClearsAfterResolve|TestPromotionGateIndependentOfTestPassAlone|TestBaselinePromotionRequiresStoredEvaluation|TestEvaluationComparesScoresToBaselineNotBoolean|TestEvaluationRegressionFlagInComparisonJSON|TestPromotionGateRequiresStoredTestNotAgentClaim'` → ok; `go test ./internal/store/... -count=1 -run 'TestBaselineStoreRoundtrip|TestOpenCreatesDBAndMigratesIdempotent|TestMigrationStatusReportsEmbedMax'` → ok; `go test ./internal/domain/... -count=1 -run 'TestSeedExportIncludesP20Cognition|TestSeedImportP20RoundTrip'` → ok; `CGO_ENABLED=1 go test ./evals/compat/... -count=1 -run TestCompatibilitySecurityChecklist` → ok. **Next P21-S04-02**. |
| 305 | P21-S04-02 | done | [phases/phase-21-thoughtprocess-completion/scopes/scope-04-baseline-promotion/02-scope-review.md](../phases/phase-21-thoughtprocess-completion/scopes/scope-04-baseline-promotion/02-scope-review.md) | 2026-08-18: **review APPROVE — confidence high**. S04-01 deliverables match repo: mig **020_baselines_promotion.sql** only (20 schema files, no **021+**); `status` CHECK active/superseded + `supersedes_id` default `''` + idx on commit+label+status; `PromoteBaseline`/`SupersedeBaseline` chain B100→B101 with `baseline.promoted` event; idempotent double-promote (single event); `CheckPromotionGate` advisory — latest computed eval wins (`latestComputedEvaluation` by `created_at`/`id` desc); reasons `eval_regression`/`no_stored_evaluation`/`baseline_not_found`; `CreateBaseline` defaults `active`; loop `promotion_blocked` wired in `Status()` — **not** merged into `statusBlocked()` (`deliberation_packet.go` L317–319 unchanged); `TransitionTask`/`task_state.go` does not call gate; seed optional `status`/`supersedes_id`; compat ceiling **20** (forbid 021+). **D-09 closed:** promote chain via `PromoteBaseline` + `supersedes_id`. **D-10 closed:** `overall_regression` → `CheckPromotionGate` + loop `promotion_blocked`; advisory only. **Tests PASS (reviewer re-run):** domain 9/9 named + P20 keepers → ok 0.394s; store mig/baseline 3/3 → ok 0.146s; seed S01 round-trip 2/2 → ok 0.217s; `cmd/trace` `TestLoopStatus|TestLoopApplyDeliberationTransitionEvent` → ok 0.237s; `CGO_ENABLED=1` compat → ok 0.918s. **Findings:** 0 blocker/high; 1 low (no dedicated CLI `promotion_blocked` regression test — mitigated by `CheckPromotionGate` unit tests + `buildPromotionBlocked` wiring). No spawns. **Next P21-S05-00**. |
| 306 | P21-S05-00 | done | [phases/phase-21-thoughtprocess-completion/scopes/scope-05-observability-why/00-PLANNER.md](../phases/phase-21-thoughtprocess-completion/scopes/scope-05-observability-why/00-PLANNER.md) | 2026-08-18: scope planner **done**. Live confirmed: S02 P20 types in `lookupEntity`; `why.go` generic `recent_event` (no transition special-case); `ImpactSummariesForWhySeed` task+decision only; `NextPacket` lacks `historical_relationships`; schema max **020**; compat **20**. Locked W-07/W-08: `ReasonDeliberationTransition` step shape; impact overlay for regression source; repo-wide historical section cap **8** (`observed_relationship` + evidence-backed `caused_by`); `trace.loop.next.v1` unchanged. 6 named tests locked. Thickened `01-observability-why.md` + `02-scope-review.md`. No product Go. **Next `P21-S05-01`**. |
| 307 | P21-S05-01 | done | [phases/phase-21-thoughtprocess-completion/scopes/scope-05-observability-why/01-observability-why.md](../phases/phase-21-thoughtprocess-completion/scopes/scope-05-observability-why/01-observability-why.md) | 2026-08-18: **observability + why landed**. `ReasonDeliberationTransition` + `deliberation.transition` parsing in task why (`title`=to_phase, `detail`=transition reason); `ImpactSummariesForWhySeed` regression source overlay + uncertainty nil; loop `historical_relationships` section (observed_relationship + evidence-backed caused_by, cap 8). No mig 021; `trace.loop.next.v1` unchanged; compat ceiling **20**. **Tests PASS:** `go test ./internal/retrieval/... -count=1 -run TestWhyUncertaintyIncludesGraphSteps` → ok; `CGO_ENABLED=1 go test ./cmd/trace -count=1 -run 'TestCLIWhyUncertainty|TestCLIWhyRegression|TestWhyTaskIncludesDeliberationTransition|TestLoopNextHistoricalRelationshipsSection|TestHistoricalRelationshipsObservedVsCaused|TestCausalWhyContextRoundTrip|TestSeedImportAndWhy|TestLoopApplyDeliberationTransitionEvent'` → ok; `go test ./internal/domain/... -count=1 -run TestCausalRelationshipFailClosedWithoutEvidence` → ok; `CGO_ENABLED=1 go test ./evals/compat/... -count=1 -run TestCompatibilitySecurityChecklist` → ok. **Next P21-S05-02**. |
| 308 | P21-S05-02 | done | [phases/phase-21-thoughtprocess-completion/scopes/scope-05-observability-why/02-scope-review.md](../phases/phase-21-thoughtprocess-completion/scopes/scope-05-observability-why/02-scope-review.md) | 2026-08-18: **review APPROVE — confidence high**. S05-01 deliverables match repo: `ReasonDeliberationTransition` in `types.go`; `why.go` special-cases `deliberation.transition` (`title`=to_phase, `detail`=transition reason_code; malformed payload falls back to `recent_event` L70–92); `ImpactSummariesForWhySeed` regression source overlay (`impact.go` L388–434) + uncertainty nil; loop `historical_relationships` top-level on `NextPacket` (`next.go` L61/L294); `buildHistoricalRelationshipsSection` caps **8**, observed always eligible, `caused_by` only with `relationship_supported_by` evidence (`deliberation_packet.go` L301–352); P20 types reuse S02 `lookupEntity` (8 cases, no duplicate registration); `NextSchemaVersion` still `trace.loop.next.v1`; schema **20** files (no **021+**); compat ceiling **20**; MCP catalog still **10** tools. **D-11 closed:** `TestCLIWhyUncertainty` + `TestCLIWhyRegression` (CLI exit 0 + steps; regression reaches `outcome_result`). **D-12 closed:** `TestLoopNextHistoricalRelationshipsSection` + `TestHistoricalRelationshipsObservedVsCaused`. **Tests PASS (reviewer re-run):** `go test ./internal/retrieval/... -count=1 -run TestWhyUncertaintyIncludesGraphSteps` → ok 0.043s; `CGO_ENABLED=1 go test ./cmd/trace -count=1 -run 'TestCLIWhyUncertainty|TestCLIWhyRegression|TestWhyTaskIncludesDeliberationTransition|TestLoopNextHistoricalRelationshipsSection|TestHistoricalRelationshipsObservedVsCaused|TestCausalWhyContextRoundTrip|TestSeedImportAndWhy|TestLoopApplyDeliberationTransitionEvent'` → ok 0.590s; `go test ./internal/domain/... -count=1 -run 'TestCausalRelationshipFailClosedWithoutEvidence|TestRegressionAttribution'` → ok 0.040s; `CGO_ENABLED=1 go test ./evals/compat/... -count=1 -run TestCompatibilitySecurityChecklist` → ok 0.942s. **Findings:** 0 blocker/high; 2 low (no dedicated malformed-transition fallback test — mitigated by `why.go` L70–92 branch + `TestWhyTaskIncludesDeliberationTransition`; keeper `-run TestRegressionAttribution` matches no test — Law 5 covered by P20 `TestSetAttributionCausedFailClosed*` + `TestHistoricalRelationshipsObservedVsCaused`). No spawns. **Next P21-S06-00**. |
| 309 | P21-S06-00 | done | [phases/phase-21-thoughtprocess-completion/scopes/scope-06-apply-hardening/00-PLANNER.md](../phases/phase-21-thoughtprocess-completion/scopes/scope-06-apply-hardening/00-PLANNER.md) | 2026-08-18: scope planner **done**. Live confirmed: `Apply` sequential writes (no tx); partial rows on mid-failure (discovery + bad replan); seed goal check at `apply.go` L375–377; `ApplyDeliberationTransition` no goal match guard; **0** `internal/loop/*_test.go`; 27 `TestLoop*` in cmd/trace; schema max **020**; compat **20**. Locked W-09/W-10/W-11: `store.WithTx` wraps all mutating apply steps; domain goal_id match; **≥8** tests in `internal/loop/apply_test.go` + cmd/trace CLI keepers; rollback scenario discovery+replan-no-discovery_id. 8 internal + 3 domain + 3 CLI named tests. Thickened `01-apply-hardening.md` + `02-scope-review.md`. No product Go. **Next `P21-S06-01`**. |
| 310 | P21-S06-01 | done | [phases/phase-21-thoughtprocess-completion/scopes/scope-06-apply-hardening/01-apply-hardening.md](../phases/phase-21-thoughtprocess-completion/scopes/scope-06-apply-hardening/01-apply-hardening.md) | 2026-08-18: **apply hardening landed**. `store.WithTx` + `runInTx` for nested FTS; `Apply` wraps all mutating steps in one tx (pre-tx: task load, seed goal mismatch, replay); `ApplyDeliberationTransition` rejects goal_id ≠ task.GoalID. **8** tests in `internal/loop/apply_test.go`; moved duplicates trimmed from `cmd/trace/loop_test.go`. No mig 021; compat ceiling **20**. **Tests PASS:** loop 8/8 → ok 0.206s; domain 3/3 → ok 0.124s; cmd/trace 5 keepers → ok 0.256s; compat → ok 0.928s. **Next P21-S06-02**. |
| 311 | P21-S06-02 | done | [phases/phase-21-thoughtprocess-completion/scopes/scope-06-apply-hardening/02-scope-review.md](../phases/phase-21-thoughtprocess-completion/scopes/scope-06-apply-hardening/02-scope-review.md) | 2026-08-18: **review APPROVE — confidence high**. S06-01 deliverables match repo: `store.WithTx` in `tx.go` (Begin/defer Rollback/Commit-on-nil); `Apply` wraps discoveries→cognitive→transition→`loop.step.applied` in one tx (`apply.go` L389–523); pre-tx boundary task load + seed goal mismatch + replay (`L371–386`); `ApplyDeliberationTransition` rejects missing/mismatched goal_id (`deliberation.go` L31–35); schema **20** files (max **020**, no **021+**); compat ceiling **20**; `ApplySchemaVersion` still `trace.loop.apply.v1`. **internal/loop test count: 8** (`TestLoopApply*`×7 + `TestValidateApplyEnvelopeSpawnedTaskGoalMismatch`). D-15: moved tests in `apply_test.go`; cmd/trace keeps 4 CLI keepers only (no verbatim duplicates). **D-08 closed:** `WithTx` + `TestLoopApplyTransactionalRollbackOnFailure` (0 discoveries/transitions after mid-failure). **D-13 closed:** `TestApplyDeliberationTransitionRequiresMatchingGoalID` + `TestLoopApplyGoalIDMismatchFailsClosed`. **D-15 closed:** 8 non-vacuous tests in `./internal/loop/...`. **Tests PASS (reviewer re-run):** loop 8/8 → ok 0.226s; domain 3/3 → ok 0.083s; cmd/trace 6 keepers → ok 0.331s; store mig → ok 0.039s; `CGO_ENABLED=1` compat → ok 0.915s. **Findings:** 0 blocker/high; 1 low (`TestLoopApplyNoPartialWritesOnValidationFailure` exercises `ValidateApplyEnvelope` only, not `Apply` — mitigated by CLI `ParseApplyEnvelope` pre-tx + tx rollback on domain errors). No spawns. **Next P21-S07-00**. |
| 312 | P21-S07-00 | done | [phases/phase-21-thoughtprocess-completion/scopes/scope-07-promoted-mvp-cuts/00-PLANNER.md](../phases/phase-21-thoughtprocess-completion/scopes/scope-07-promoted-mvp-cuts/00-PLANNER.md) | 2026-08-18: scope planner **done**. Live confirmed: schema max **020** (20 files, no 021); no `experiments` table; `next.go` lacks `risk_hints`; compat ceiling **20** (forbid 021+); outcome kinds unchanged (test/verification/evaluation). Locked W-12/W-13: mig **021_experiments.sql** thin table (status lifecycle + optional outcome_result_id); deterministic `risk_hints[]` on loop next (4 codes, cap 4, priority blocking→missing_verification→many_paths→high_churn_path; thresholds >8 paths / ≥3 churn / blocking count / verification debt); compat **21** (forbid 022+); no seed/MCP/runner. 6 named tests locked. Thickened `01-promoted-mvp-cuts.md` + `02-scope-review.md`. No product Go. **Next `P21-S07-01`**. |
| 313 | P21-S07-01 | done | [phases/phase-21-thoughtprocess-completion/scopes/scope-07-promoted-mvp-cuts/01-promoted-mvp-cuts.md](../phases/phase-21-thoughtprocess-completion/scopes/scope-07-promoted-mvp-cuts/01-promoted-mvp-cuts.md) | 2026-08-18: **promoted MVP cuts landed**. Mig **021_experiments.sql** (thin experiments table + store/domain CRUD + `ListHighChurnPaths`); deterministic `risk_hints[]` on loop next (4 codes, cap 4, priority blocking→missing_verification→many_paths→high_churn_path); `trace.loop.next.v1` unchanged; compat ceiling **21** (forbid 022+); no seed/MCP/runner. **Tests PASS:** `go test ./internal/domain/... -count=1 -run 'TestCreateExperimentLinksOutcome|TestExperimentStatusLifecycle|TestNoExperimentRunnerInvoked'` → ok 0.089s; `go test ./internal/loop/... -count=1 -run 'TestRiskHintsManyPaths|TestRiskHintsBlockingUncertainty|TestLoopNextRiskHintsBounded'` → ok 0.110s; `go test ./internal/store/... -count=1 -run 'TestMigrationStatusReportsEmbedMax|TestOpenCreatesDBAndMigratesIdempotent'` → ok 0.057s; `CGO_ENABLED=1 go test ./evals/compat/... -count=1 -run TestCompatibilitySecurityChecklist` → ok 0.918s. **Next P21-S07-02**. |
| 314 | P21-S07-02 | done | [phases/phase-21-thoughtprocess-completion/scopes/scope-07-promoted-mvp-cuts/02-scope-review.md](../phases/phase-21-thoughtprocess-completion/scopes/scope-07-promoted-mvp-cuts/02-scope-review.md) | 2026-08-18: **review APPROVE — confidence high**. S07-01 deliverables match repo: mig **021_experiments.sql** only (**21** schema files, no **022+**); thin table (metadata + `outcome_result_id` link, no blob/candidate/runner fields); status CHECK `planned`\|`running`\|`completed`; forward-only lifecycle; domain CRUD only (no `os/exec` in `experiments.go`); `risk_hints` top-level on `NextPacket` (`next.go` L62/L260/L301); hint shape `{code, severity, detail}`; locked four codes only; cap **4**; priority blocking→missing_verification→many_paths→high_churn_path (`risk_hints.go` L13–19/L41–90); thresholds >8 paths / ≥3 churn; `select.go` untouched (advisory only); `NextSchemaVersion` still `trace.loop.next.v1`; compat ceiling **21**; seed export unchanged (no `experiments` key); MCP catalog still **10** tools. **D-01 closed:** thin experiment records — not bake-off engine. **D-02 closed:** deterministic risk hints — not ML selection engine. **Tests PASS (reviewer re-run):** domain 3/3 → ok 0.113s; loop 3/3 named + 8/8 apply keepers → ok 0.124s/0.188s; store mig 2/2 → ok 0.078s; `cmd/trace` `TestLoopNextPacketShape|TestLoopNextHistoricalRelationshipsSection` → ok 0.176s; `CGO_ENABLED=1` compat → ok 1.017s. **Findings:** 0 blocker/high; 2 low (no isolated `missing_verification`/`high_churn_path` subtests — mitigated by bounded priority test + threshold constants; `TestLoopNextPacketShape` omits `risk_hints` key — mitigated by `next.go` wiring + loop named tests). No spawns. **Next P21-S08-00**. |
| 315 | P21-S08-00 | done | [phases/phase-21-thoughtprocess-completion/scopes/scope-08-phase-verify/00-PLANNER.md](../phases/phase-21-thoughtprocess-completion/scopes/scope-08-phase-verify/00-PLANNER.md) | 2026-08-18: scope planner **done**. Live confirmed: schema max **021** (21 files, no 022+); compat ceiling **21**; S01–S07 surfaces landed (seed P20 keys, 14-row SelectNext, promotion mig 020, tx apply ≥8 tests, experiments + risk_hints). Locked W-14: Block A full P20 S07-01 floor + Block B P21 named keepers + Block C compat **21**; §31 **live** mini-eval **≥5 phases** (D-14 promoted, not fixture-only); seed **includes** P20 (D-05 retired). DR-HANDOFF close S08-02: **`no successor`** default unless human names Phase 22 — **never TBD**. Thickened `01-verify.md` + `02-scope-review.md`. No product Go. **Next `P21-S08-01`**. |
| 316 | P21-S08-01 | done | [phases/phase-21-thoughtprocess-completion/scopes/scope-08-phase-verify/01-verify.md](../phases/phase-21-thoughtprocess-completion/scopes/scope-08-phase-verify/01-verify.md) | 2026-08-18: **phase verify PASS**. Evidence: `experiments/runs/2026-08-18-p21-s08-01-verify/evidence/` + [`VERIFY-NOTES.md`](../phases/phase-21-thoughtprocess-completion/scopes/scope-08-phase-verify/VERIFY-NOTES.md). Block A P20 floor 10/10 PASS; Block B S01–S07 12/12 PASS (`b-s04-promotion` fixed flaky gate via `UpsertOutcomeResult` `created_at` on conflict); Block C compat **21** PASS. §31 **live** mini-eval **5 phases** (CRITIQUE/INVESTIGATE/VERIFY/ORIENT/STOP) — `02-full-cycle-next.json`. CLI: P20 seed export/import (`01-seed-export-p20-grep.txt`), why/risk_hints/historical/promotion_blocked/tx rollback artifacts. Fixes: `seed.go` P20 import whitelist; `outcomes.go` created_at upsert. `trace/graph.json` exported. Optional product bar FAIL (MCP mod download sandbox). DR-HANDOFF **OPEN**. **Next `P21-S08-02`**. |
| 317 | P21-S08-02 | done | [phases/phase-21-thoughtprocess-completion/scopes/scope-08-phase-verify/02-scope-review.md](../phases/phase-21-thoughtprocess-completion/scopes/scope-08-phase-verify/02-scope-review.md) | 2026-08-18: **review APPROVE — confidence high**. Independent re-verify floor PASS (Block A P20 + Block B S01–S07 + Block C compat **21**); S08-01 evidence reviewed (CLI 01–07 + 99; §31 live mini-eval **5 phases** CRITIQUE/INVESTIGATE/VERIFY/ORIENT/STOP). Schema **21** files (no 022+); MCP **10** tools; 8 `internal/loop` apply tests. **Findings:** 0 blocker/high; 2 low (optional product bar sandbox flake; BuildPolicyInputs cycle flags stubbed in live CLI — unit tests cover 14-row cycle). **DR-HANDOFF CLOSED** — successor **`no successor`**. Phase 21 **complete**. No spawn. **Next: none** (human promotes forward work). |

---
