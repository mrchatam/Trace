Index: [`docs/TODO.md`](../TODO.md)

## Phase 22 — Capability completion

**Status:** complete (closed 2026-08-18). DR-HANDOFF **CLOSED** — **`no successor`**. Next runnable: **none**.

Human-promoted forward follow-on after Phase 21 close. Closed **all 43** remaining unchecked items in [`CAPABILITIES_CHECKLIST.md`](../CAPABILITIES_CHECKLIST.md) plus **S09 harness agent enhancements** (E01–E04). Work map: [`WORK-MAP.md`](../phases/phase-22-capability-completion/WORK-MAP.md). Coverage matrix: [`README.md`](../phases/phase-22-capability-completion/README.md). **No post-MVP leftovers at close.**

| Order | ID | Status | Prompt | Notes |
|------:|----|--------|--------|-------|
| 318 | P22-00 | done | [phases/phase-22-capability-completion/00-PHASE-PLANNER.md](../phases/phase-22-capability-completion/00-PHASE-PLANNER.md) | 2026-08-18: phase planner **done**. Live inventory: schema max **021**; MCP **10** tools; `BuildPolicyInputs` stubs cycle flags; install cursor+claude only; FTS unwired to CLI; no `validates` edges; DF-86 absent. Coverage matrix 43/43 owned. Scopes S01–S08 thickened. DR-HANDOFF **OPEN**. No product Go. **Next `P22-S01-00`**. |
| 319 | P22-S01-00 | done | [phases/phase-22-capability-completion/scopes/scope-01-test-code-graph/00-PLANNER.md](../phases/phase-22-capability-completion/scopes/scope-01-test-code-graph/00-PLANNER.md) | 2026-08-18: scope planner **done**. Live: schema max **021** (no `code_edges`); `IndexFile` = UpsertFile+SetFileLanguage+ReplaceFileSymbols+ReplaceFileImports; ImpactWalk seeds file\|symbol only; compat ceiling **21**. Thickened 01–08: live paths (`extract_javascript.go` not extract_js.go; no `file_graph_test.go` yet); test heuristics locked to real queries (JS/TS need new `call_expression`); mig **022** / compat **22**; no 023+ this scope. **No product Go**. **Next `P22-S01-01`**. |
| 320 | P22-S01-01 | done | [phases/phase-22-capability-completion/scopes/scope-01-test-code-graph/01-test-validates-graph.md](../phases/phase-22-capability-completion/scopes/scope-01-test-code-graph/01-test-validates-graph.md) | 2026-08-18: implementer **done**. C03 **true** (checklist boxed). Mig **022** `code_edges`; compat/embed **22** (no 023+). Tests as `kind=test`; `validates` EXTRACTED from imports + INFERRED `heuristic:go_test_name_prefix`; `ReplaceFileEdges` outgoing-only; target IndexFile upserts incoming via `UpsertFilePairEdges`. JS/TS `extractJSTSTestCalls` (`call_expression`). Deterministic symbol IDs. **PASS:** `go test ./internal/analyzers/... ./internal/store/... -count=1 -run 'TestIndexDiscoversGoTestFunctions|TestValidatesEdgeExtractedFromImport|TestReplaceFileEdgesIsFileLocal|TestIndexPythonAndTSTestFiles|TestOpenCreatesDBAndMigratesIdempotent|TestMigrationStatusReportsEmbedMax|TestNoSourceContentColumns|TestIncrementalIsolation|TestIndexFileGoGolden'`; `CGO_ENABLED=1 go test ./evals/compat/... -count=1 -run TestCompatibilitySecurityChecklist`. Also PASS: `TestValidatesInferredGoNamePrefix`. Files: `022_code_relationships.sql`, `file_graph.go`+test, `index.go`, `test_graph.go`, `extract_jsts_tests.go`, `extract_go.go`, analyzers testdata, compat/store embed-max bumps. **Next `P22-S01-02`**. |
| 321 | P22-S01-02 | done | [phases/phase-22-capability-completion/scopes/scope-01-test-code-graph/02-test-validates-review.md](../phases/phase-22-capability-completion/scopes/scope-01-test-code-graph/02-test-validates-review.md) | 2026-08-18: independent review **done**. Confidence **high**. C03 **closed**. Checklist: mig **022** only (22 sql files, no 023); embed/compat **22**; file-local `TestReplaceFileEdgesIsFileLocal`+`TestIncrementalIsolation`; EXTRACTED vs INFERRED (`heuristic:go_test_name_prefix`); JS/TS tree-sitter `call_expression` not regex; `Extract([]byte)` API v1; `TestNoSourceContentColumns`; SeedDocument has no `code_edges`; `ListValidatesForSymbol`; symbol FK `ON DELETE SET NULL`. **high** inline-fixed: incoming upsert passed empty goPkg so `package foo_test` without import got dishonest INFERRED; persist `kind=package` on Go `_test.go` and read it back (`TestValidatesFooTestPackageNotInferred`, `TestValidatesIncomingSamePackageStillInferred`). SET NULL vs deterministic IDs: target reindex repaired by incoming upsert (`TestValidatesSurviveTargetReindex` PASS). Non-test IndexFile still skips `ReplaceFileEdges` — **thickened** upcoming `P22-S01-03` (must `ReplaceFileEdges` every path, merge validates+contains+exports). **low** residual: `TestOutcomeResultsSchemaNoBlobColumns` FAIL baselines columns `status`,`supersedes_id` (mig 020, not this row). No spawn 02a/02b. **Re-run PASS:** `go test ./internal/analyzers/... ./internal/store/... -count=1 -run 'TestIndexDiscoversGoTestFunctions|TestValidatesEdgeExtractedFromImport|TestReplaceFileEdgesIsFileLocal|TestIndexPythonAndTSTestFiles|TestOpenCreatesDBAndMigratesIdempotent|TestMigrationStatusReportsEmbedMax|TestNoSourceContentColumns|TestIncrementalIsolation|TestIndexFileGoGolden|TestValidatesInferredGoNamePrefix|TestValidatesSurviveTargetReindex|TestValidatesFooTestPackageNotInferred|TestValidatesIncomingSamePackageStillInferred|TestLanguageAdapterAPIVersion'` → `ok analyzers 0.488s` `ok store 0.146s`. `CGO_ENABLED=1 go test ./evals/compat/... -count=1 -run TestCompatibilitySecurityChecklist` → `ok evals/compat 0.940s`. **Next `P22-S01-03`**. |
| 322 | P22-S01-03 | done | [phases/phase-22-capability-completion/scopes/scope-01-test-code-graph/03-artifact-relationships.md](../phases/phase-22-capability-completion/scopes/scope-01-test-code-graph/03-artifact-relationships.md) | 2026-08-18: implementer **done**. C01 **true** (checklist boxed; tests via S01-01). No 023+ (schema max **022**); compat stays **22**. `IndexFile` calls `ReplaceFileEdges` for **every** path; one outgoing batch = `validates` + `contains_module` + `exports_api`. Pre-clear outgoing before `ReplaceFileSymbols` (SET NULL on `to_symbol_id` would collide `idx_code_edges_unique`). Export: Go `unicode.IsUpper` on function/method/type; Python name not `_`; JS/TS tree-sitter `export_statement` (not regex). `ListExports` / `ListModuleContents` (dir = module identity). Go `_test.go` still `kind=package`. No `depends_on` clone of `imports`. Seed untouched. `LanguageAdapterAPIVersion=1`. **PASS:** `go test ./internal/analyzers/... ./internal/store/... -count=1 -run 'TestArtifactEdgesFunctionsTypesAPIs\|TestModuleContainsSymbols\|TestReplaceFileEdgesIsFileLocal\|TestIndexDiscoversGoTestFunctions\|TestValidatesFooTestPackageNotInferred\|TestListExportsAndModuleContents'` → `ok analyzers 0.217s` `ok store 0.071s`. Broader: `go test ./internal/analyzers/... -count=1` → `ok 0.931s` (S01-01 keepers incl. goldens, validates, `TestLanguageAdapterAPIVersion`). Store keepers `TestOpenCreatesDBAndMigratesIdempotent\|TestMigrationStatusReportsEmbedMax\|TestNoSourceContentColumns` PASS. Files: `artifact_graph.go` (new), `index.go`, `test_graph.go`, `extract_javascript.go`, `analyzers_test.go`, `file_graph.go`+test, `CAPABILITIES_CHECKLIST.md` C01. **Next `P22-S01-04`**. |
| 323 | P22-S01-04 | done | [phases/phase-22-capability-completion/scopes/scope-01-test-code-graph/04-artifact-relationships-review.md](../phases/phase-22-capability-completion/scopes/scope-01-test-code-graph/04-artifact-relationships-review.md) | 2026-08-18: independent review **done**. Confidence **high**. C01 **closed**. **high** inline-fixed: unique-collide on SET NULL **confirmed** — two named-import `validates` from one test (`import { alpha, beta }`) to one lib made `IndexFile` of the lib fail (`idx_code_edges_unique`). `ReplaceFileSymbols` was DELETE-all then INSERT. Fix: upsert-first (stable deterministic ids keep incoming FKs) + collapse leftover incoming before leftover DELETE. Tests: `TestValidatesMultipleNamedImportsSurviveTargetReindex`, `TestReplaceFileSymbolsKeepsIncomingValidatesOnStableIDs`, `TestReplaceFileSymbolsCollapsesIncomingWhenSymbolDropped`. **high** landmine **thickened** upcoming S01-05/06 (no 04a spawn): `IndexFile` pre-clears **all** outgoing then rewrites only validates+contains_module+exports_api — `architectural_boundary` **must** join that same `ReplaceFileEdges` batch or it is deleted; locked Outgoing merge / Second replace / from_file_id; named `TestArchitecturalBoundarySurvivesFileReindex`. **medium** accepted (locked export rules): JS `export { foo }` is **not** `exports_api` — honest EXTRACTED (declaration-under-`export_statement` only); asserted in `TestArtifactEdgesFunctionsTypesAPIs`; `export { foo } from` re-exports also omitted. **C01 components:** language-idiomatic packages/modules (dir identity, `ListModuleContents` / `TestModuleContainsSymbols`); no `modules`/`components` tables — satisfies C01 under planner lock. No `depends_on` writes (const+CHECK only). Schema max **022** (22 sql files); compat **22**. No spawn 04a/04b. **Re-run PASS:** `go test ./internal/analyzers/... ./internal/store/... -count=1 -run 'TestArtifactEdgesFunctionsTypesAPIs\|TestModuleContainsSymbols\|TestIndexDiscoversGoTestFunctions\|TestValidatesEdgeExtractedFromImport\|TestReplaceFileEdgesIsFileLocal\|TestValidatesMultipleNamedImportsSurviveTargetReindex\|TestReplaceFileSymbolsKeepsIncomingValidatesOnStableIDs\|TestReplaceFileSymbolsCollapsesIncomingWhenSymbolDropped'` → `ok analyzers 0.420s` `ok store 0.180s`. Extra keepers `TestValidatesFooTestPackageNotInferred\|TestListExportsAndModuleContents\|TestLanguageAdapterAPIVersion\|TestValidatesSurviveTargetReindex` PASS. `CGO_ENABLED=1 go test ./evals/compat/... -count=1 -run TestCompatibilitySecurityChecklist` → `ok evals/compat 0.983s`. **Next `P22-S01-05`**. |
| 324 | P22-S01-05 | done | [phases/phase-22-capability-completion/scopes/scope-01-test-code-graph/05-architectural-boundaries.md](../phases/phase-22-capability-completion/scopes/scope-01-test-code-graph/05-architectural-boundaries.md) | 2026-08-18: implementer **done**. C02 **true** (checklist boxed). No 023+ (schema max **022**); compat stays **22**. `indexCodeEdges` appends `architectural_boundary` into the **same** outgoing `ReplaceFileEdges` batch as validates+contains_module+exports_api (pre-clear then one rewrite; no second replace). from_file_id = indexed source file. Layer identity TO stubs at `architecture/<layer>` (never IndexFile'd; not FROM). EXTRACTED when `go.mod` present or optional `trace/architecture.json` overlay; INFERRED from `cmd/` `internal/` `pkg/` path segments. Incremental: dirty file path + root `go.mod`/`architecture.json` only (no repo glob). Cross-layer imports left as `imports`; `ListCrossLayerImports` joins membership. Helpers: `FileLayer`, `ListArchitecturalBoundaries`, `ListCrossLayerImports`. **PASS:** `go test ./internal/analyzers/... ./internal/store/... -count=1 -run 'TestArchitecturalBoundaryEdges\|TestArchitecturalBoundaryIncremental\|TestArchitecturalBoundarySurvivesFileReindex\|TestReplaceFileEdgesIsFileLocal'` → `ok analyzers 0.508s` `ok store 0.046s`. Keepers `TestArtifactEdgesFunctionsTypesAPIs\|TestModuleContainsSymbols\|TestIndexDiscoversGoTestFunctions\|TestValidatesEdgeExtractedFromImport\|TestIndexFileGoGolden\|TestIncrementalIsolation` PASS. `go test ./internal/analyzers/... -count=1` → `ok 2.842s`. `CGO_ENABLED=1 go test ./evals/compat/... -count=1 -run TestCompatibilitySecurityChecklist` → `ok 1.498s`. Files: `architecture_graph.go` (new), `test_graph.go`, `index.go`, `analyzers_test.go`, `file_graph.go`, C02 checkbox. **Next `P22-S01-06`**. |
| 325 | P22-S01-06 | done | [phases/phase-22-capability-completion/scopes/scope-01-test-code-graph/06-architectural-boundaries-review.md](../phases/phase-22-capability-completion/scopes/scope-01-test-code-graph/06-architectural-boundaries-review.md) | 2026-08-18: independent review **done**. Confidence **high**. C02 **closed**. Law 12: `IndexFile` is the incremental unit (dirty path + root `go.mod`/`architecture.json` only; no repo glob / no full-graph rebuild). Outgoing merge: `IndexFile` `ReplaceFileEdges(path, nil)` pre-clear then **one** rewrite in `indexCodeEdges` (validates+contains_module+exports_api+architectural_boundary); grep: no second product `ReplaceFileEdges`. TO-only layer stubs at `architecture/<layer>` (FK `to_file_id`); FROM is always the indexed source file. EXTRACTED = overlay match **or** project-root `go.mod` with a `module ` line (module-path string is **not** stored); INFERRED = conventional `cmd/` `internal/` `pkg/` without those. Cross-layer imports stay `imports` (`ListCrossLayerImports` join; no `depends_on` clone). Schema max **022** (22 sql files, no 023+); compat **22**. No graph DB / LLM layers. **medium** inline: optional overlay had no named test — added `TestArchitecturalBoundaryOverlayExtracted` (overlay EXTRACTED `platform` TO `architecture/platform`; unmatched `cmd` INFERRED). Also asserted TO path + stub-not-FROM in `TestArchitecturalBoundaryEdges`; INFERRED without go.mod in `TestArchitecturalBoundaryIncremental`. **thickened** upcoming S01-07/08: do not walk `architectural_boundary` as an impact hop; overlay keeper on S01-08 regex. **low** accepted: go.mod EXTRACTED is presence of `module ` at root, not parsed module path. `TestOutcomeResultsSchemaNoBlobColumns` is pre-existing (mig 020), not this row. No spawn 06a/06b. **Re-run PASS:** `go test ./internal/analyzers/... ./internal/store/... -count=1 -run 'TestArchitecturalBoundaryEdges\|TestArchitecturalBoundaryIncremental\|TestArchitecturalBoundarySurvivesFileReindex\|TestArchitecturalBoundaryOverlayExtracted\|TestReplaceFileEdgesIsFileLocal\|TestArtifactEdgesFunctionsTypesAPIs\|TestModuleContainsSymbols\|TestIndexDiscoversGoTestFunctions\|TestValidatesEdgeExtractedFromImport'` → `ok analyzers 1.298s` `ok store 0.169s`. `CGO_ENABLED=1 go test ./evals/compat/... -count=1 -run TestCompatibilitySecurityChecklist` → `ok evals/compat 1.468s`. **Next `P22-S01-07`**. |
| 326 | P22-S01-07 | done | [phases/phase-22-capability-completion/scopes/scope-01-test-code-graph/07-impact-affected-tests.md](../phases/phase-22-capability-completion/scopes/scope-01-test-code-graph/07-impact-affected-tests.md) | 2026-08-18: implementer **done**. C07 **true** (checklist boxed). Reverse `validates` in `impactNeighborsSymbol`/`impactNeighborsFile` via `ListValidatesForSymbol`+`ListValidatesForFile`; tests in blast as `entity_type=symbol` with `edge_provenance`; `ImpactWalkResult.AffectedTests` JSON `affected_tests` = filter of kept blast (no extra hops/cap). Depth 1..2 unchanged; `TestImpactWalkDepthStillCapped` depth 3 fail-closed. CLI/MCP thin encode `affected_tests`; catalog still **10**. No `architectural_boundary` hop. **PASS:** `go test ./internal/retrieval/... -count=1 -run 'TestImpactWalk'` → `ok 0.495s`; `CGO_ENABLED=1 go test ./cmd/trace/... -count=1 -run 'TestImpact'` → `ok 0.144s`; `go test ./internal/mcp/... -count=1 -run 'TestToolNamesRegistered'` → `ok 0.013s`. Files: `impact_walk.go`+test, `file_graph.go` (`ListValidatesForFile`), `cmd/trace/impact.go`, `internal/mcp/tools_impact.go`, C07 checkbox. **Next `P22-S01-08`**. |
| 327 | P22-S01-08 | done | [phases/phase-22-capability-completion/scopes/scope-01-test-code-graph/08-impact-affected-tests-review.md](../phases/phase-22-capability-completion/scopes/scope-01-test-code-graph/08-impact-affected-tests-review.md) | 2026-08-18: independent review **done**. Confidence **high**. C07 **closed**; C01–C03 **hold** (keeper re-run). Reverse `validates` in `impactNeighborsSymbol` (`ListValidatesForSymbol`) and `impactNeighborsFile` (`listReverseValidatesForFile` + `ListValidatesForFile`); `allowContainsOut` asymmetry: file seed expands symbol-level + file-level validates; after contains-UP only file-level (`to_symbol_id` nil). `AffectedTests`/`affected_tests` = filter of **kept** blast via `seen.symbolKind=="test"` (no extra hops/cap). `ListValidatesForFile` belongs in `file_graph.go` (mirror of `ListValidatesForSymbol`; used only from retrieval). No `architectural_boundary` hop — grep `internal/retrieval`: zero refs; neighbors = contains + incoming imports + reverse validates only. Seeds still `file\|symbol`; MCP catalog **10**; no `trace tests` CLI; schema **22** (22 sql files); compat **22**. Overlay keeper `TestArchitecturalBoundaryOverlayExtracted` PASS. **low** accepted: no named test for file-seed `affected_tests` or explicit architecture-stub exclusion in blast (code path + grep sufficient); `ListValidatesForFile` lacks dedicated store unit test (SQL mirrors symbol variant). No spawn 08a/08b. **Re-run PASS:** `go test ./internal/retrieval/... -count=1 -run 'TestImpactWalk'` → `ok 1.934s`; `go test ./internal/analyzers/... ./internal/store/... -count=1 -run 'TestIndexDiscoversGoTestFunctions\|TestValidatesEdgeExtractedFromImport\|TestArtifactEdgesFunctionsTypesAPIs\|TestArchitecturalBoundaryEdges\|TestArchitecturalBoundaryOverlayExtracted\|TestReplaceFileEdgesIsFileLocal'` → `ok analyzers 0.594s` `ok store 0.102s`; `CGO_ENABLED=1 go test ./evals/compat/... -count=1 -run TestCompatibilitySecurityChecklist` → `ok 1.475s`; `go test ./internal/mcp/... -count=1 -run 'TestToolNamesRegistered'` → `ok 0.031s`. **S01 complete.** **Next `P22-S02-00`**. |
| 328 | P22-S02-00 | done | [phases/phase-22-capability-completion/scopes/scope-02-sync-change-compare/00-PLANNER.md](../phases/phase-22-capability-completion/scopes/scope-02-sync-change-compare/00-PLANNER.md) | 2026-08-18: scope planner **done**. Live: schema max **022** (22 sql); compat **22**; install **cursor+claude only** (DF-86 git-hook absent); `buildIndexHonesty` disk-only; `MetaVCSWatermark` VCS index separate from graph sync; no `PromoteVCSCommitToChange` / `CompareStates` / `trace changes`. Locked: hook id **`git-hook`** CONDITIONAL; hooks dir via **`git rev-parse --git-path hooks`** (core.hooksPath); post-commit+optional pre-push; markers `# begin-trace`/`# end-trace`; never wrap commit; mig **023_graph_sync.sql** / compat **23** after S02-03; `CompareStates`; VCS promote `source_type=VCS`, `task_id=trace:vcs-capture`. Thickened 01–08. No product Go. **Next `P22-S02-01`**. |
| 329 | P22-S02-01 | done | [phases/phase-22-capability-completion/scopes/scope-02-sync-change-compare/01-git-hook-sync.md](../phases/phase-22-capability-completion/scopes/scope-02-sync-change-compare/01-git-hook-sync.md) | 2026-08-18: implementer **done**. DF-86 **promoted**: `install.TargetGitHook` CONDITIONAL; `internal/install/githook.go` marker-gated post-commit + pre-push via `git rev-parse --git-path hooks` (honors `core.hooksPath`); hook indexes changed paths + optional `seed export`; `command -v trace \|\| exit 0`; never wraps `git commit`. CLI `trace install git-hook [--write]` + help/usage. CONTRIBUTING hook paragraph updated. No 023+ (schema max **022**); compat stays **22**. **PASS:** `go test ./internal/install/... -count=1 -run 'TestInstallGitHook\|TestUninstallGitHook\|TestInstallDetectIncludesGitHook'` → `ok 0.236s`; `CGO_ENABLED=1 go test ./cmd/trace/... -count=1 -run 'TestHelp\|TestInstall'` → `ok 0.019s`. Files: `githook.go`+test, `types.go`, `registry.go`, `cmd/trace/install.go`, `help.go`, `CONTRIBUTING.md`. **Next `P22-S02-02`**. |
| 330 | P22-S02-02 | done | [phases/phase-22-capability-completion/scopes/scope-02-sync-change-compare/02-git-hook-sync-review.md](../phases/phase-22-capability-completion/scopes/scope-02-sync-change-compare/02-git-hook-sync-review.md) | 2026-08-18: independent review **done**. Confidence **high**. C25 **closed** (checklist boxed). DF-86 / D-22-16 **hold**: post-commit+pre-push marker fragments only; **`git rev-parse --git-path hooks`** in `resolveHooksDir` (honors `core.hooksPath` — `TestInstallGitHookHonorsCoreHooksPath`); `# begin-trace`/`# end-trace` calls **`trace -C … index`** + optional **`seed export`** with `\|\| true`; uninstall strips markers, preserves user lines (`TestUninstallGitHookRemovesFragment`); zero `exec.Command("git","commit")` / no daemon / no HTTP in `internal/install`. Detect: **`git-hook`** tier **CONDITIONAL** (`TestInstallDetectIncludesGitHook`). Schema max **022** (22 sql files, no 023+); compat **22** PASS. CLI/help list `git-hook`; CONTRIBUTING DF-86 hook paragraph + manual index recipe. **low** accepted: no named test asserts `seed export` line in hook body (present in fragment); print-only `--write false` untested (claude/cursor pattern). No spawn 02a/02b. **Re-run PASS:** `go test ./internal/install/... -count=1 -run 'TestInstallGitHook\|TestUninstallGitHook\|TestInstallDetectIncludesGitHook'` → `ok 0.238s`; `CGO_ENABLED=1 go test ./cmd/trace/... -count=1 -run 'TestHelp\|TestInstall'` → `ok 0.022s`; `CGO_ENABLED=1 go test ./evals/compat/... -count=1 -run TestCompatibilitySecurityChecklist` → `ok 0.983s`. **Next `P22-S02-03`**. |
| 331 | P22-S02-03 | done | [phases/phase-22-capability-completion/scopes/scope-02-sync-change-compare/03-index-honesty.md](../phases/phase-22-capability-completion/scopes/scope-02-sync-change-compare/03-index-honesty.md) | 2026-08-18: implementer **done**. C04 **true** (checklist boxed). Mig **023** `graph_sync_state` (single row id=1: `last_indexed_commit`, `last_indexed_at`, `hook_installed`); separate from `vcs_meta`/`MetaVCSWatermark`. Compat/embed **23** (no 024+). `GetGraphSyncState`/`UpsertGraphSyncState`; `cmdIndex` watermark after success when git present; `trace index status` JSON; `GraphSyncHonesty` on packet + MD banner; loop `contextSectionFreshness` dirty on commit lag; `hook_installed` on `install git-hook --write` / cleared on uninstall. Disk `IndexHonesty` unchanged. **PASS:** `go test ./internal/store/... ./internal/compiler/... ./cmd/trace/... -count=1 -run 'TestGraphSyncStaleWhenHeadDiffers\|TestHookIndexUpdatesLastIndexedCommit\|TestMigrationStatusReportsEmbedMax\|TestOpenCreatesDBAndMigratesIdempotent'` → `ok store 0.149s` `ok cmd/trace 0.228s`; `CGO_ENABLED=1 go test ./evals/compat/... -count=1 -run TestCompatibilitySecurityChecklist` → `ok 0.975s`; keepers `TestIndexStaleBanner\|TestIndexHonestyStaleTotalTruncated` PASS. Files: `023_graph_sync.sql`, `graph_sync.go`+test, `index.go`, `index_status.go`, `graph_sync_honesty.go`, `packet.go`, `compiler.go`, `next.go`, `install.go`, `help.go`, compat/store embed bumps, C04 checkbox. **Next `P22-S02-04`**. |
| 332 | P22-S02-04 | done | [phases/phase-22-capability-completion/scopes/scope-02-sync-change-compare/04-index-honesty-review.md](../phases/phase-22-capability-completion/scopes/scope-02-sync-change-compare/04-index-honesty-review.md) | 2026-08-18: independent review **done**. Confidence **high**. C04 **closed** (checklist `[x]` holds). Schema max **23** exactly (23 sql files, no 024+); compat/embed **23** PASS. `graph_sync_state` singleton (id=1) separate from `vcs_meta`/`MetaVCSWatermark` — zero cross-wiring in index/graph-sync path; VCS watermark stays in `gitcli/refresh.go`. `trace index status` JSON reports `head`, `last_indexed_commit`, `stale`, `hook_installed`. `GraphSyncHonesty` on packet + MD banner; `contextSectionFreshness` marks **dirty** on `StaleCommit` (like disk stale). Watermark via `updateGraphSyncWatermark` only **after** successful `cmdIndex` loop (`UpsertGraphSyncState` not called on mid-loop fail). Disk `IndexHonesty` unchanged (`buildIndexHonesty` untouched). No new full-rebuild glob for graph sync (Law 12). **low** accepted: no named loop unit test for commit-lag → `FreshnessDirty` (code path + grep sufficient, mirrors S01-08); no test asserting watermark skipped on index hard-fail; `hook_installed` flag set/cleared in `install.go` without dedicated keeper. No spawn 04a/04b. **Re-run PASS:** `go test ./internal/store/... ./internal/compiler/... ./cmd/trace/... -count=1 -run 'TestGraphSyncStaleWhenHeadDiffers\|TestHookIndexUpdatesLastIndexedCommit\|TestOpenCreatesDBAndMigratesIdempotent\|TestIndexStaleBanner'` → `ok store 0.286s` `ok compiler 0.320s` `ok cmd/trace 0.300s`; `CGO_ENABLED=1 go test ./evals/compat/... -count=1 -run TestCompatibilitySecurityChecklist` → `ok evals/compat 1.042s`; `ls internal/store/schema/*.sql \| wc -l` → **23**. **Next `P22-S02-05`**. |
| 333 | P22-S02-05 | done | [phases/phase-22-capability-completion/scopes/scope-02-sync-change-compare/05-change-capture.md](../phases/phase-22-capability-completion/scopes/scope-02-sync-change-compare/05-change-capture.md) | 2026-08-18: implementer **done**. C05 **true** (checklist boxed). No 024+ (schema max **23**); compat stays **23**. `GetChangeByGitCommit`; `PromoteVCSCommitToChange` idempotent on OID; paths from `vcs_commit_paths` (no blobs); `source_type=VCS`, `task_id=trace:vcs-capture`, `reason=subject`, `status=RECORDED`; meaningful filter (`DetectLanguage` or indexed file) + `--all`; skip zero paths (not error). CLI `trace changes capture [--since <oid>] [--all]`; `compare` stub (S02-07); `cli:changes` AUTO_ALLOW; post-index `promoteVCSCommitsAfterIndex` best-effort. **PASS:** `go test ./internal/domain/... -count=1 -run 'TestPromoteVCSCommit\|TestCreateChangeWithGitSHAAndPathsNoBlob'` → `ok 0.250s`; `CGO_ENABLED=1 go test ./cmd/trace/... -count=1 -run 'TestChangesCapture'` → `ok 0.246s`; `go test ./internal/loop/... -count=1 -run TestLoopApplyDeliberationTransitionEvent` → `ok 0.044s`. Files: `changes.go` (domain/store/cmd), `changes_test.go`, `vcs_index.go`, `capability.go`, `index.go`, `root.go`, `help.go`, C05 checkbox. **Next `P22-S02-06`**. |
| 334 | P22-S02-06 | done | [phases/phase-22-capability-completion/scopes/scope-02-sync-change-compare/06-change-capture-review.md](../phases/phase-22-capability-completion/scopes/scope-02-sync-change-compare/06-change-capture-review.md) | 2026-08-18: independent review **done**. Confidence **high**. C05 **closed** (checklist `[x]` holds). VCS promote: `PromoteVCSCommitToChange`/`GetChangeByGitCommit` idempotent on OID (`TestPromoteVCSCommitCreatesChangeIdempotent` + CLI second-capture); `source_type=VCS`, `task_id=trace:vcs-capture`, `reason=subject`, `status=RECORDED`; paths via `ListIndexedCommitPaths` → `vcs_commit_paths` only (`TestPromoteVCSCommitRecordsPathsNoBlob`, `HasBlobLikeColumns`); meaningful filter `DetectLanguage`∨indexed file + `--all` (`TestPromoteVCSCommitSkipsNonMeaningful`, `TestChangesCaptureSkipsDocsOnlyUnlessAll`); zero paths → empty Change/nil (not error). CLI `trace changes capture [--since] [--all]`; `compare` stub (S02-07); `cli:changes` builtin AUTO_ALLOW; post-index `promoteVCSCommitsAfterIndex` best-effort stderr-only (Refresh untouched). Schema max **23** (23 sql, no 024+); compat **23** PASS. Loop apply path unchanged (`applyChange` direct upsert; `TestLoopApplyDeliberationTransitionEvent` PASS). Refresh noop: keeper is `TestIncrementalRefresh` (review prompt `-run TestRefresh` matches zero — rerun with correct name). **low** accepted: no named test for post-index auto-promote or `--since`; JSON row omits `source_type`. No spawn 06a/06b. **Re-run PASS:** `go test ./internal/domain/... -count=1 -run 'TestPromoteVCSCommit\|TestCreateChangeWithGitSHAAndPathsNoBlob'` → `ok 0.301s`; `CGO_ENABLED=1 go test ./cmd/trace/... -count=1 -run 'TestChangesCapture\|TestLoopApplyDeliberationTransitionEvent'` → `ok 0.695s`; `go test ./internal/gitcli/... -count=1 -run TestIncrementalRefresh` → `ok 0.147s`; `go test ./internal/loop/... -count=1 -run TestLoopApplyDeliberationTransitionEvent` → `ok 0.040s`; `CGO_ENABLED=1 go test ./evals/compat/... -count=1 -run TestCompatibilitySecurityChecklist` → `ok 0.956s`. **Next `P22-S02-07`**. |
| 335 | P22-S02-07 | done | [phases/phase-22-capability-completion/scopes/scope-02-sync-change-compare/07-state-compare.md](../phases/phase-22-capability-completion/scopes/scope-02-sync-change-compare/07-state-compare.md) | 2026-08-18: implementer **done**. C06 **true** (checklist unboxed — S02-08 review closes). No 024+ (schema max **23**); compat stays **23**. `CompareStates` in `domain/compare.go`; `vcs.Repository.DiffNameStatus` + `gitcli` `git diff --name-status from..to` (no blobs); `change_ids` via `GetChangeByGitCommit` on range+endpoints; invalid OID fail-closed. CLI `trace changes compare --from <oid> --to <oid>` JSON stdout; `cli:changes` gated. **PASS:** `go test ./internal/domain/... -count=1 -run 'TestCompareStates'` → `ok 0.526s`; `CGO_ENABLED=1 go test ./cmd/trace/... -count=1 -run 'TestChangesCompare'` → `ok 0.161s`; `CGO_ENABLED=1 go test ./evals/compat/... -count=1 -run TestCompatibilitySecurityChecklist` → `ok 1.275s`. Files: `compare.go`+test, `history.go`, `repository.go`, `fake.go`, `changes.go`+test, `help.go`. **Next `P22-S02-08`**. |
| 336 | P22-S02-08 | done | [phases/phase-22-capability-completion/scopes/scope-02-sync-change-compare/08-state-compare-review.md](../phases/phase-22-capability-completion/scopes/scope-02-sync-change-compare/08-state-compare-review.md) | 2026-08-18: independent review **done**. Confidence **high**. **S02 complete** — C04/C05/C06/C25 **closed** (checklist all four `[x]`). **C06:** `CompareStates` in `domain/compare.go` — `git diff --name-status from..to` via `gitcli.DiffNameStatus`; `change_ids` from `GetChangeByGitCommit` on endpoints+range; invalid OID fail-closed (`TestCompareStatesUnknownOIDFailClosed`); CLI `trace changes compare --from --to` JSON (`TestChangesCompare`); `HasBlobLikeColumns` in path-delta test. **C04 hold:** `graph_sync_state` singleton separate from `vcs_meta`; `GraphSyncHonesty` + loop freshness dirty on HEAD≠watermark; watermark after successful index only. **C05 hold:** VCS promote idempotent, paths-only, meaningful filter; loop apply unchanged. **C25 hold:** `git-hook` CONDITIONAL; marker fragments; `git rev-parse --git-path hooks`; never wraps commit (`TestInstallGitHookDoesNotWrapCommit`); CONTRIBUTING DF-86. Schema max **23** (23 sql, no 024+); compat **23** PASS. S01 C01–C03/C07 spot-check PASS. MCP catalog **10**. No daemon/HTTP/`git commit` wrap in install. **low** accepted (carry-forward): no named test for hook `seed export` line; post-index auto-promote/`--since`; loop commit-lag freshness unit test; capture JSON omits `source_type`. No spawn 08a/08b. **Re-run PASS:** `go test ./internal/domain/... ./internal/install/... ./internal/store/... ./internal/compiler/... -count=1 -run 'TestCompareStates\|TestPromoteVCSCommit\|TestInstallGitHook\|TestGraphSyncStaleWhenHeadDiffers'` → `ok domain 0.752s` `ok install 0.071s`; `CGO_ENABLED=1 go test ./evals/compat/... -count=1 -run TestCompatibilitySecurityChecklist` → `ok 1.324s`; `go test ./internal/mcp/... -count=1 -run TestToolNamesRegistered` → `ok 0.011s`; S01 keepers `go test ./internal/retrieval/... ./internal/analyzers/... -count=1 -run 'TestImpactWalk\|TestIndexDiscoversGoTestFunctions\|TestValidatesEdgeExtractedFromImport\|TestArtifactEdgesFunctionsTypesAPIs\|TestArchitecturalBoundaryEdges'` → `ok retrieval 0.665s` `ok analyzers 0.592s`. **Next `P22-S03-00`**. |
| 337 | P22-S03-00 | done | [phases/phase-22-capability-completion/scopes/scope-03-deliberation-verify/00-PLANNER.md](../phases/phase-22-capability-completion/scopes/scope-03-deliberation-verify/00-PLANNER.md) | 2026-08-18: scope planner **done**. Live: schema max **23** (23 sql); compat **23**; `BuildPolicyInputs` stubs cycle flags (sets debt/regression/plan only); `select.go` 14-row FINAL honors flags; outcomes gates + `HasImplementationSignal`/`HasVerificationDebt` live; no `internal/testrun`; no `trace test`/`verify` CLI; status has `verification_debt`+`promotion_blocked` only; S01 validates/impact + S02 changes available. Locked: no SQL (reuse outcomes/changes/reflections); compat stays **23**; precise flag queries (test since latest change; reflect since eval timestamp); testrun+coordinate+invariants CLI. Thickened 01–08. **No product Go**. **Next `P22-S03-01`**. |
| 338 | P22-S03-01 | done | [phases/phase-22-capability-completion/scopes/scope-03-deliberation-verify/01-policy-cycle-flags.md](../phases/phase-22-capability-completion/scopes/scope-03-deliberation-verify/01-policy-cycle-flags.md) | 2026-08-18: implementer **done**. C09 **true** (checklist **not** boxed — S03-02 review closes). No 024+ (schema max **23**); compat stays **23**. `BuildPolicyInputs` sets `execute_pending`/`test_pending`/`evaluation_pending`/`reflect_pending` from live store queries; `VerificationIncomplete` still `HasVerificationDebt`; `ReplanNeeded` false + `OpenDecisionAlternatives` 0 stubs; `select.go` untouched. Domain helpers: `HasTestOutcomeSinceLatestChange`, `HasComputedEvaluation`, `HasVerificationOutcome`, `HasReflectionSinceEvaluation`. **PASS:** `go test ./internal/loop/... ./internal/deliberation/... ./internal/domain/... -count=1 -run 'TestBuildPolicyInputs\|TestSelectNext\|TestVerificationDebtWhenImplementationWithoutVerification'` → `ok loop 0.466s` `ok deliberation 0.006s` `ok domain 0.099s`; `CGO_ENABLED=1 go test ./cmd/trace -count=1 -run 'TestLoopNextExecute\|TestLoopStatusDeliberationFields\|TestLoopNextDeliberationSectionPresent'` → `ok 0.725s`; `CGO_ENABLED=1 go test ./evals/compat/... -count=1 -run TestCompatibilitySecurityChecklist` → `ok 1.424s`. Files: `policy.go`+test (new), `outcomes.go`+test, `loop_test.go`. **Next `P22-S03-02`**. |
| 339 | P22-S03-02 | done | [phases/phase-22-capability-completion/scopes/scope-03-deliberation-verify/02-policy-cycle-flags-review.md](../phases/phase-22-capability-completion/scopes/scope-03-deliberation-verify/02-policy-cycle-flags-review.md) | 2026-08-18: independent review **done**. Confidence **high**. C09 **closed** (checklist boxed). **C09:** `BuildPolicyInputs` assigns all four cycle flags from live domain queries — `ExecutePending` (plan+critique+gates+!impl), `TestPending` (`HasImplementationSignal` && !`HasTestOutcomeSinceLatestChange`), `EvaluationPending` (`CheckVerificationGate` \|\| `HasVerificationOutcome`) && !`HasComputedEvaluation`, `ReflectPending` (`HasComputedEvaluation` && !`HasReflectionSinceEvaluation`); `VerificationIncomplete` still `HasVerificationDebt`; `ReplanNeeded`/`OpenDecisionAlternatives` remain intentional stubs. **Semantics:** test-since-latest-change ignores stale pre-change rows (`TestHasTestOutcomeSinceLatestChange`); reflect uses eval `created_at` vs reflection `created_at` (`TestHasReflectionSinceEvaluation`). **`select.go`:** 14-row P21 FINAL order unchanged (EXECUTE→TEST→VERIFY→EVALUATE→REFLECT rows 8–12). Schema max **23** (23 sql, no 024+); compat **23**. **low** accepted: CLI keeper `TestLoopNextExecuteWhenPendingLive` covers execute path only — test/eval/reflect flags proven via unit tests + domain helpers. No spawn 02a/02b. **Re-run PASS:** `go test ./internal/loop/... ./internal/deliberation/... ./internal/domain/... -count=1 -run 'TestBuildPolicyInputs\|TestSelectNextNeverExecuteOnBlockingUncertainty\|TestVerificationDebtWhenImplementationWithoutVerification'` → `ok loop 0.529s` `ok deliberation 0.006s` `ok domain 0.109s`; `CGO_ENABLED=1 go test ./cmd/trace -count=1 -run 'TestLoopNextExecute\|TestLoopStatusDeliberationFields'` → `ok 0.595s`; `CGO_ENABLED=1 go test ./cmd/trace -count=1 -run TestLoopNextExecuteWhenPendingLive` → `ok 0.165s`; `CGO_ENABLED=1 go test ./evals/compat/... -count=1 -run TestCompatibilitySecurityChecklist` → `ok 1.426s`; `ls internal/store/schema/*.sql \| wc -l` → **23**. **Next `P22-S03-03`**. |
| 340 | P22-S03-03 | done | [phases/phase-22-capability-completion/scopes/scope-03-deliberation-verify/03-test-invoke.md](../phases/phase-22-capability-completion/scopes/scope-03-deliberation-verify/03-test-invoke.md) | 2026-08-18: implementer **done**. C12 **true** (checklist unboxed — S03-04 review closes); C38-CLI test half **true** (`trace test run`). No 024+ (schema max **23**); compat stays **23**. New `internal/testrun`: `RunRelevantTests`, stub `Runner`, impact/validates selection + Go package fallback, optional `trace/test-runner.json`, default `go test` per target (5m ctx timeout), always `RecordTestOutcome`; fail-closed without go.mod/config. CLI `trace test run --task <id> [--paths path,...]` JSON; `cli:test` AUTO_ALLOW; help/root wired. **PASS:** `go test ./internal/testrun/... ./internal/domain/... -count=1 -run 'TestTestRun|TestRecordTestOutcome|TestPromotionGateRequiresStoredTestNotAgentClaim'` → `ok testrun 0.691s` `ok domain 0.188s`; `CGO_ENABLED=1 go test ./cmd/trace/... -count=1 -run 'TestTestRun|TestHelpIncludesTestRun'` → `ok 0.593s`; `CGO_ENABLED=1 go test ./evals/compat/... -count=1 -run TestCompatibilitySecurityChecklist` → `ok 1.292s`. Files: `internal/testrun/{runner,config,select,run}*.go`, `cmd/trace/test.go`+test, `root.go`, `help.go`, `capability.go`. **Next `P22-S03-04`**. |
| 341 | P22-S03-04 | done | [phases/phase-22-capability-completion/scopes/scope-03-deliberation-verify/04-test-invoke-review.md](../phases/phase-22-capability-completion/scopes/scope-03-deliberation-verify/04-test-invoke-review.md) | 2026-08-18: independent review **done**. Confidence **high**. C12 **closed** (checklist boxed). C38-CLI **test half closed** (`trace test run`; MCP half deferred S08 — checklist C38 unboxed). **C12:** `internal/testrun` + `cmd/trace/test.go`; `RunRelevantTests` invokes subprocess via `ExecRunner`/`Runner` stub; every executed target calls `domain.RecordTestOutcome` (pass/fail/error — no stdout-only pass); fail-closed without `go.mod`/`trace/test-runner.json` (`TestTestRunFailClosedWithoutCommand`). **Selection:** impact `AffectedTests` + `ListValidatesForFile` + Go package fallback (`TestTestRunSelectsValidatingTests`, `TestSelectTargetsFromImpactWalk`, `TestPackageFallbackTargets`). **C09 hold:** `TestTestRunClearsTestPending` — `test_pending` clears after recorded outcome. **Law 2:** `TestPromotionGateRequiresStoredTestNotAgentClaim` PASS. **No daemon:** zero `net/http`/ListenAndServe/background watcher in `internal/testrun`. Schema max **23** (23 sql); compat **23** PASS; MCP catalog **10** (`TestToolNamesRegistered`). `cli:test` in `BuiltinCLICapabilitySpecs` AUTO_ALLOW; help/root wired. **low** accepted: custom `test-runner.json` re-runs same command per target (planner lock); no named unit test for custom-config path; `resolveDefaultRunner` `!ok` branch unreachable when `err` set. No spawn 04a/04b. **Re-run PASS:** `go test ./internal/testrun/... ./internal/domain/... -count=1 -run 'TestTestRun|TestPromotionGateRequiresStoredTestNotAgentClaim'` → `ok testrun 0.548s` `ok domain 0.096s`; `CGO_ENABLED=1 go test ./cmd/trace/... -count=1 -run 'TestTestRun'` → `ok 0.718s`; `go test ./internal/mcp/... -count=1 -run TestToolNamesRegistered` → `ok 0.013s`; `CGO_ENABLED=1 go test ./evals/compat/... -count=1 -run TestCompatibilitySecurityChecklist` → `ok 1.217s`. **Next `P22-S03-05`**. |
| 342 | P22-S03-05 | done | [phases/phase-22-capability-completion/scopes/scope-03-deliberation-verify/05-verification-cycle.md](../phases/phase-22-capability-completion/scopes/scope-03-deliberation-verify/05-verification-cycle.md) | 2026-08-18: implementer **done**. C11/C13/C36 **true** (checklist unboxed — S03-06 review closes). No 024+ (schema max **23**); compat stays **23**. `trace.loop.status.v1` adds `verification_cycle` block (`execute_pending`, `test_pending`, `verification_incomplete`, `evaluation_pending`, `reflect_pending`, `incomplete_reason`, optional `regression_detected`/`test_name`); `statusBlocked` unchanged. `domain.CoordinateVerification` orders test→verify→eval (fail-closed on test fail; `--force-eval` override); `DetectTestRegression`/`DetectAnyTestRegression` (C13); `testrun.CoordinateTestRun` bridges import cycle. CLI `trace verify run --task <id> [--force-eval]` JSON; `cli:verify` AUTO_ALLOW. **PASS:** `go test ./internal/loop/... ./internal/domain/... -count=1 -run 'TestVerificationCycle|TestCoordinateVerification|TestRegressionDetectedVsPriorPassingTest|TestTestPassAloneCannotSatisfyVerificationGate|TestVerificationDebt'` → `ok loop 0.057s` `ok domain 0.200s`; `CGO_ENABLED=1 go test ./cmd/trace -count=1 -run 'TestLoopStatus|TestVerifyRun|TestHelpIncludesVerifyRun'` → `ok 0.271s`. Files: `internal/domain/coordinate.go`+test, `internal/loop/verification_cycle.go`+test, `internal/loop/apply.go`, `internal/testrun/coordinate.go`, `cmd/trace/verify.go`+test, `root.go`, `help.go`, `capability.go`. **Next `P22-S03-06`**. |
| 343 | P22-S03-06 | done | [phases/phase-22-capability-completion/scopes/scope-03-deliberation-verify/06-verification-cycle-review.md](../phases/phase-22-capability-completion/scopes/scope-03-deliberation-verify/06-verification-cycle-review.md) | 2026-08-18: independent review **done**. Confidence **medium** (C13 blocker). **C11 hold:** `verification_cycle` block on `trace.loop.status.v1` mirrors `PolicyInputs` (`execute_pending`, `test_pending`, `verification_incomplete`, `evaluation_pending`, `reflect_pending`, `incomplete_reason`); `buildVerificationCycle` wires `DetectAnyTestRegression` → `regression_detected`/`test_name`; `TestVerificationCycleBlocksSkipInStatus` PASS — `test_pending` + incomplete_reason when impl without test row. **`select.go`:** EXECUTE→TEST→VERIFY→EVALUATE order unchanged; cycle flags block skip. **C36 hold:** `domain.CoordinateVerification` test→verify→eval; `TestCoordinateVerificationOrder` + stop-on-fail/`--force-eval` PASS; CLI `trace verify run` JSON (`TestVerifyRunCLI`); `testrun.CoordinateTestRun` bridges import cycle. **D-22-20 hold:** zero `TransitionTask` in `coordinate.go`/`verify.go`/`internal/loop`. **C13 FAIL (high):** `TestRegressionDetectedVsPriorPassingTest` **flaky** — 18/30 FAIL (`-count=1` loop). Root cause: `created_at` second-precision RFC3339; tie-break uses UUID lex order not insertion order → pass/fail pair inverted ~60%. `DetectTestRegression` re-sorts matching rows (`coordinate.go` L84–91). C13 not closed — checklist **unboxed**. **C09/C12 hold:** `TestBuildPolicyInputs` PASS; `TestTestPassAloneCannotSatisfyVerificationGate` PASS; `internal/testrun` keeper PASS. Schema max **23**; compat **23** PASS. **Spawn:** `P22-S03-06a` (ordering fix) + `P22-S03-06b` (re-review + box). **Re-run:** loop/domain keepers PASS (single run); regression keeper flaky as above; `CGO_ENABLED=1 go test ./cmd/trace -count=1 -run 'TestLoopStatus\|TestVerifyRun'` → `ok`; compat → `ok`. **Next `P22-S03-06a`**. |
| 344 | P22-S03-06a | done | [phases/phase-22-capability-completion/scopes/scope-03-deliberation-verify/06a-regression-ordering-fix.md](../phases/phase-22-capability-completion/scopes/scope-03-deliberation-verify/06a-regression-ordering-fix.md) | 2026-08-18: implementer **done**. C13 ordering fix (checklist **unboxed** — S03-06b closes). No 024+ (schema max **23**); compat stays **23**. `ListOutcomeResultsByTaskKind` tie-break `ORDER BY created_at ASC, rowid ASC` (insertion order when second-precision `created_at` collides); dropped redundant UUID re-sort in `DetectTestRegression`. **PASS:** `go test ./internal/domain/... -count=30 -run TestRegressionDetectedVsPriorPassingTest` → `ok 0.925s` (30/30 stable); `go test ./internal/domain/... -count=1 -run 'TestRegressionDetectedSameSecondTimestamps|TestRegressionDetectedVsPriorPassingTest'` → `ok`; keepers `go test ./internal/loop/... ./internal/domain/... -count=1 -run 'TestVerificationCycle\|TestCoordinateVerification\|TestRegressionDetected\|TestTestPassAloneCannotSatisfyVerificationGate'` → `ok loop 0.037s` `ok domain 0.183s`; `CGO_ENABLED=1 go test ./cmd/trace -count=1 -run 'TestLoopStatus\|TestVerifyRun'` → `ok 0.278s`. Files: `internal/store/outcomes.go`, `internal/domain/coordinate.go`, `internal/domain/coordinate_test.go`. **Next `P22-S03-06b`**. |
| 345 | P22-S03-06b | done | [phases/phase-22-capability-completion/scopes/scope-03-deliberation-verify/06b-regression-ordering-fix-review.md](../phases/phase-22-capability-completion/scopes/scope-03-deliberation-verify/06b-regression-ordering-fix-review.md) | 2026-08-18: independent review **done**. Confidence **high**. **C11/C13/C36 closed** (checklist boxed). **C13:** `ListOutcomeResultsByTaskKind` tie-break `ORDER BY created_at ASC, rowid ASC`; redundant UUID re-sort removed from `DetectTestRegression`; `TestRegressionDetectedVsPriorPassingTest` **30/30** stable; `TestRegressionDetectedSameSecondTimestamps` PASS (UUID-lex fail/pass same-second). **C11 hold:** `verification_cycle` block mirrors `PolicyInputs`; `buildVerificationCycle` → `DetectAnyTestRegression`; `TestVerificationCycleBlocksSkipInStatus` PASS. **C36 hold:** `CoordinateVerification` test→verify→eval; `TestCoordinateVerificationOrder` + stop-on-fail/`--force-eval` PASS; CLI `TestVerifyRun` PASS. **D-22-20:** zero `TransitionTask` in `coordinate.go`/`verify.go`/`internal/loop`. **C09/C12 hold:** `TestBuildPolicyInputs` + `TestTestPassAloneCannotSatisfyVerificationGate` PASS. Schema max **23** (23 sql); compat **23** PASS. No spawn 06c/06d. **Re-run PASS:** `go test ./internal/domain/... -count=30 -run TestRegressionDetectedVsPriorPassingTest` → `ok 0.944s`; `go test ./internal/loop/... ./internal/domain/... -count=1 -run 'TestVerificationCycle\|TestCoordinateVerification\|TestRegressionDetected\|TestTestPassAloneCannotSatisfyVerificationGate\|TestBuildPolicyInputs'` → `ok loop 0.234s` `ok domain 0.219s`; `CGO_ENABLED=1 go test ./cmd/trace -count=1 -run 'TestLoopStatus\|TestVerifyRun'` → `ok 0.319s`; `CGO_ENABLED=1 go test ./evals/compat/... -count=1 -run TestCompatibilitySecurityChecklist` → `ok 0.987s`. **Next `P22-S03-07`**. |
| 346 | P22-S03-07 | done | [phases/phase-22-capability-completion/scopes/scope-03-deliberation-verify/07-invariants-iterations.md](../phases/phase-22-capability-completion/scopes/scope-03-deliberation-verify/07-invariants-iterations.md) | 2026-08-18: implementer **done**. C14/C15 **true** (checklist **unboxed** — S03-08 review closes). No 024+ (schema max **23**); compat stays **23**. `CheckArchitecturalInvariants` default rule **internal→cmd** on latest RECORDED/COMPARED change paths via `ListCrossLayerImports` (no repo glob). Failed check is **advisory JSON only** (CLI exit 0; no auto `kind=verification` row). `CompareIterationOutcomes` last two stored rows of `kind` test\|evaluation → `{previous,current,delta}` (test_status and/or score dimension deltas; no re-exec). CLI `trace verify invariants --task`; `trace outcomes compare --task --kind test\|evaluation`; `cli:outcomes` AUTO_ALLOW. **PASS:** `go test ./internal/domain/... ./internal/analyzers/... -count=1 -run 'TestInvariant\|TestCompareIterationOutcomes\|TestArchitecturalBoundaryEdges'` → ok domain 0.141s ok analyzers 0.049s; `CGO_ENABLED=1 go test ./cmd/trace/... -count=1 -run 'TestVerifyInvariants\|TestOutcomesCompare'` → ok 0.148s; `CGO_ENABLED=1 go test ./evals/compat/... -count=1 -run TestCompatibilitySecurityChecklist` → ok 0.948s; schema sql **23**. Keepers `TestVerifyRun\|TestHelpIncludesVerify` PASS; full `go test ./internal/domain/...` ok 4.138s. Files: `invariants.go`+test, `outcomes.go`+test, `cmd/trace/{verify,outcomes,root,help}.go`, `capability.go`, analyzers testdata architecture fixtures. **Residuals for S03-08:** optional verification-row on fail not written (locked advisory JSON); compare is last-two-of-kind not same `test_name`; one default rule (S07 extends). **Next `P22-S03-08`**. |
| 347 | P22-S03-08 | done | [phases/phase-22-capability-completion/scopes/scope-03-deliberation-verify/08-invariants-iterations-review.md](../phases/phase-22-capability-completion/scopes/scope-03-deliberation-verify/08-invariants-iterations-review.md) | 2026-08-18: independent review **done**. Confidence **high**. **S03 complete** — C09, C11–C15, C36 **closed** (checklist boxed). **C14:** `CheckArchitecturalInvariants` default rule `internal_must_not_import_cmd` on latest RECORDED/COMPARED change paths via indexed `ListCrossLayerImports` + touched-path filter (no repo glob); advisory JSON only (`verify invariants` exit 0 on fail; no auto verification row). **C15:** `CompareIterationOutcomes` last two stored rows of `kind` test\|evaluation (`ORDER BY created_at ASC, rowid ASC`) → `{previous,current,delta}`; pure stored rows (no re-exec). CLI `trace verify invariants` + `trace outcomes compare`; `cli:verify`/`cli:outcomes` in `BuiltinCLICapabilitySpecs`. **C09 hold:** live cycle flags in `BuildPolicyInputs`. **C11 hold:** `verification_cycle` status block + skip gate (`TestVerificationCycleBlocksSkipInStatus`). **C12 hold:** `trace test run` + stored outcomes. **C13 hold:** `DetectTestRegression` insertion-order tie-break; `TestRegressionDetectedVsPriorPassingTest` **30/30** stable. **C36 hold:** `CoordinateVerification` test→verify→eval; zero `TransitionTask` in coordinate/verify/loop. **C38-CLI:** test/verify/outcomes CLI closed; checklist **C38** unboxed (MCP half deferred S08). Schema max **23**; compat **23** PASS; MCP catalog **10**. **low** accepted (planner lock): advisory-only invariant fail; compare last-two-of-kind not per-`test_name`; one default rule (S07 extends). No spawn 08a/08b. **Re-run PASS:** `go test ./internal/loop/... ./internal/deliberation/... ./internal/domain/... ./internal/testrun/... -count=1 -run 'TestBuildPolicyInputs\|TestSelectNextNeverExecuteOnBlockingUncertainty\|TestTestRun\|TestVerificationCycle\|TestCoordinateVerification\|TestInvariant\|TestCompareIterationOutcomes\|TestTestPassAloneCannotSatisfyVerificationGate\|TestPromotionGateRequiresStoredTestNotAgentClaim'` → ok loop 0.268s ok domain 0.319s ok testrun 0.222s; `CGO_ENABLED=1 go test ./cmd/trace/... -count=1 -run 'TestLoopNextExecute\|TestTestRun\|TestVerifyRun\|TestVerifyInvariants\|TestOutcomesCompare'` → ok 0.697s; compat → ok 0.973s; MCP → ok 0.005s; retrieval `TestImpactWalkIncludesAffectedTests` → ok 0.036s. **Next `P22-S04-00`**. |
| 348 | P22-S04-00 | done | [phases/phase-22-capability-completion/scopes/scope-04-impact-regression-improve/00-PLANNER.md](../phases/phase-22-capability-completion/scopes/scope-04-impact-regression-improve/00-PLANNER.md) | 2026-08-18: scope planner **done**. Live: schema max **23** (23 sql); compat **23**; `ImpactWalk`+`AffectedTests` (no snapshot on change); `effects` expected/actual per dimension; regressions create **`correlated`**, `SetRegressionAttributionCaused` fail-closed, **no regression↔change link**; no improvements table/API; CLI `impact walk` only. Locked: mig **024** (`impact_predictions`+`improvements`); regression association via **`regression_associated_change`** entity_link (no ALTER regressions); compat **24** after S04-01; predict/compare blast-key sets; CLI `impact predict|compare`; improvements seed export; MCP stays **10**. Thickened 01–06. **No product Go**. **Next `P22-S04-01`**. |
| 349 | P22-S04-01 | done | [phases/phase-22-capability-completion/scopes/scope-04-impact-regression-improve/01-predicted-vs-actual.md](../phases/phase-22-capability-completion/scopes/scope-04-impact-regression-improve/01-predicted-vs-actual.md) | 2026-08-18: implementer **done**. C08 **true** (checklist **unboxed** — S04-02 review closes). Mig **024** `impact_predictions`+`improvements`; compat/embed **24** (no 025+). Domain `RecordPredictedImpact`/`PredictImpactForChange`/`CompareActualImpact` + store upsert/compare; `retrieval.WireDomainImpactWalker` breaks domain↔retrieval cycle. Predict JSON keys `file\|symbol:<uuid>`; compare `{matched,unexpected,missed}` + `compared_at`; fail-closed no row/unindexed path. CLI `trace impact predict\|compare`; MCP catalog **10**. **PASS:** `go test ./internal/domain/... ./internal/store/... ./internal/retrieval/... -count=1 -run 'TestRecordPredictedImpact\|TestImpactCompare\|TestOpenCreatesDBAndMigratesIdempotent\|TestMigrationStatusReportsEmbedMax\|TestImpactWalkIncludesAffectedTests'` → ok domain 0.107s ok store 0.071s ok retrieval 0.040s; `CGO_ENABLED=1 go test ./evals/compat/... -count=1 -run TestCompatibilitySecurityChecklist` → ok 0.957s; `CGO_ENABLED=1 go test ./cmd/trace/... -count=1 -run 'TestImpact'` → ok 0.131s; `ls internal/store/schema/*.sql \| wc -l` → **24**. Files: `024_impact_compare.sql`, `store/impact_compare.go`, `domain/impact_compare.go`+test, `retrieval/impact_bridge.go`, `cmd/trace/impact.go`, `help.go`, compat/store embed bumps. **Next `P22-S04-02`**. |
| 350 | P22-S04-02 | done | [phases/phase-22-capability-completion/scopes/scope-04-impact-regression-improve/02-predicted-vs-actual-review.md](../phases/phase-22-capability-completion/scopes/scope-04-impact-regression-improve/02-predicted-vs-actual-review.md) | 2026-08-18: independent review **done**. Confidence **high**. **C08 closed** (checklist boxed). **Schema:** exactly **24** sql files; **`024_impact_compare.sql`** only new mig (no 025+). **Compat/embed 24** PASS. **Storage:** `impact_predictions` row keyed by `change_id`; `predicted_json` on predict upsert; `compare_json`+`compared_at` persisted after compare (`TestRecordPredictedImpactThenCompareActual`, store roundtrip). **No blobs:** keys only `file\|symbol:<uuid>` in JSON payloads; tests use paths for graph fixtures only. **Walk reuse:** `CompareActualImpact` → `ImpactWalker.ImpactWalk` via `retrieval.WireDomainImpactWalker`/`DomainImpactWalker` — zero ad-hoc SQL graph in domain compare. **Affected tests:** `affected_test_keys` in `PredictedImpactPayload`; merged into blast-key diff sets; S01 keeper `TestImpactWalkIncludesAffectedTests` PASS. **Fail-closed:** compare without row → `ErrValidation` (`TestImpactCompareFailClosedWithoutPrediction`); unindexed path predict → `ErrValidation` (`TestPredictImpactForChangeFailClosedUnindexedPath`). **Effects boundary:** C08 uses `impact_predictions` only — no `effects.expected/actual` reads/writes in `impact_compare.go`. **MCP catalog 10** (`TestToolNamesRegistered`). **S01/S03 hold:** `TestRegressionDetectedVsPriorPassingTest` **30/30** stable. **low** accepted: no named test asserting `affected_test_keys` in compare delta when tests diverge (walk keeper + payload field + merge logic sufficient). No spawn 02a/02b. **Re-run PASS:** `go test ./internal/domain/... ./internal/store/... ./internal/retrieval/... -count=1 -run 'TestRecordPredictedImpact\|TestImpactCompare\|TestImpactWalkIncludesAffectedTests'` → ok domain 0.123s ok store (no match) ok retrieval 0.042s; `CGO_ENABLED=1 go test ./evals/compat/... -count=1 -run TestCompatibilitySecurityChecklist` → ok 0.985s; `CGO_ENABLED=1 go test ./cmd/trace/... -count=1 -run 'TestImpact'` → ok 0.154s; MCP → ok 0.005s; `ls internal/store/schema/*.sql \| wc -l` → **24**. **Next `P22-S04-03`**. |
| 351 | P22-S04-03 | done | [phases/phase-22-capability-completion/scopes/scope-04-impact-regression-improve/03-regression-attribution.md](../phases/phase-22-capability-completion/scopes/scope-04-impact-regression-improve/03-regression-attribution.md) | 2026-08-18: implementer **done**. C16 **true** (checklist **unboxed** — S04-04 review closes). No 025+ (schema max **24**); compat stays **24**. `RelRegressionAssociatedChange` entity_link (`regression`→`change`); auto-link on `RecordRegressionFromContradictedEffect` (idempotent, stays **correlated**); `AssociateRegressionWithChange` + `ListRegressionsByChangeID`/`RegressionsForChange`; evaluation regressions manual associate only; `SetRegressionAttributionCaused` unchanged. Seed import allowlist for `regression_associated_change`. **PASS:** `go test ./internal/domain/... -count=1 -run 'TestRegression\|TestSetAttributionCaused\|TestCorrelationAndContradiction\|TestListRegressionsByChange'` → ok 0.296s; `CGO_ENABLED=1 go test ./evals/compat/... -count=1 -run TestCompatibilitySecurityChecklist` → ok 0.927s; `ls internal/store/schema/*.sql \| wc -l` → **24**. Files: `regressions.go`+test, `service.go`, `store/regressions.go`, `seed_import.go`. **Next `P22-S04-04`**. |
| 352 | P22-S04-04 | done | [phases/phase-22-capability-completion/scopes/scope-04-impact-regression-improve/04-regression-attribution-review.md](../phases/phase-22-capability-completion/scopes/scope-04-impact-regression-improve/04-regression-attribution-review.md) | 2026-08-18: independent review **done**. Confidence **high**. **C16 closed** (checklist boxed). **Law 5:** grep confirms sole product path to `attribution=caused` is `SetRegressionAttributionCaused` (hypothesized + CONFIRMED hypothesis + evidence IDs); store create rejects non-`correlated`; `RecordCausalRelationship`/`caused_by` does not mutate attribution (`TestCausalRelationshipFailClosedWithoutEvidence`). **Association:** `regression_associated_change` in `entity_links` (no regressions column); auto-link idempotent on `RecordRegressionFromContradictedEffect` stays correlated; manual `AssociateRegressionWithChange` for evaluation source; `ListRegressionsByChangeID`/`RegressionsForChange` join query (empty slice when no links). Schema max **24** (24 sql, no 025+); compat **24** PASS. **C08 hold:** `TestRecordPredictedImpact`/`TestImpactCompare` PASS. **low** accepted: no named test for existing change with zero linked regressions (INNER JOIN + `scanRegressions` nil→empty sufficient); CLI query deferred S05-03 per planner lock. No spawn 04a/04b. **Re-run PASS:** `go test ./internal/domain/... -count=1 -run 'TestRegression\|TestSetAttributionCaused\|TestCorrelationAndContradiction\|TestListRegressionsByChange'` → ok 0.344s; `go test ./internal/domain/... -count=1 -run 'TestRecordPredictedImpact\|TestImpactCompare'` → ok 0.101s; `CGO_ENABLED=1 go test ./evals/compat/... -count=1 -run TestCompatibilitySecurityChecklist` → ok 0.980s; `ls internal/store/schema/*.sql \| wc -l` → **24**. **Next `P22-S04-05`**. |
| 353 | P22-S04-05 | done | [phases/phase-22-capability-completion/scopes/scope-04-impact-regression-improve/05-record-improvements.md](../phases/phase-22-capability-completion/scopes/scope-04-impact-regression-improve/05-record-improvements.md) | 2026-08-18: implementer **done**. C18 **true** (checklist **unboxed** — S04-06 review closes). No 025+ (schema max **24**); compat stays **24**. `RecordImprovement`/`GetImprovement`/`ListImprovementsByChangeID`/`ListImprovementsByTaskID`; store `internal/store/improvements.go`; evidence ids validated + JSON column + optional `improvement_supported_by` links. CLI `trace outcomes improvements --change <id>` \| `--task <id>` JSON. Seed `improvements[]` export/import round-trip stable ids. **PASS:** `go test ./internal/domain/... -count=1 -run 'TestRecordImprovement\|TestSeedExportIncludesImprovements\|TestSeedExportIncludesP20Cognition'` → ok 0.161s; `CGO_ENABLED=1 go test ./cmd/trace/... -count=1 -run 'TestOutcomes'` → ok 0.116s; `CGO_ENABLED=1 go test ./evals/compat/... -count=1 -run TestCompatibilitySecurityChecklist` → ok 0.929s; `ls internal/store/schema/*.sql \| wc -l` → **24**. Files: `improvements.go` (domain+store), `seed_export.go`+`seed_import.go`, `outcomes.go`+test, `help.go`, `service.go`, checklist C18 note. **Next `P22-S04-06`**. |
| 354 | P22-S04-06 | done | [phases/phase-22-capability-completion/scopes/scope-04-impact-regression-improve/06-record-improvements-review.md](../phases/phase-22-capability-completion/scopes/scope-04-impact-regression-improve/06-record-improvements-review.md) | 2026-08-18: independent review **done**. Confidence **high**. **S04 complete** — C08/C16/C18 **closed** (checklist all three `[x]`). **C18:** `RecordImprovement`/`GetImprovement`/`ListImprovementsByChangeID`/`ListImprovementsByTaskID`; store `internal/store/improvements.go`; evidence ids validated (≤32) + JSON column + `improvement_supported_by` links on record; summary cap 4096; task_id must match change.task_id; CLI `trace outcomes improvements --change\|--task` JSON (`TestOutcomesImprovementsCLI`); seed `improvements[]` export/import stable ids (`TestSeedExportIncludesImprovements`). **C08 hold:** `impact_predictions` predict/compare persisted; `TestRecordPredictedImpact`/`TestImpactCompare` PASS; S04-02 boxed. **C16 hold:** `regression_associated_change` links; `ListRegressionsByChangeID`; Law 5 sole path `SetRegressionAttributionCaused`; S04-04 boxed. Schema max **24** (24 sql, no 025+); compat/embed **24** PASS. MCP catalog **10**. **low** accepted: no named test for invalid evidence id or task/change mismatch on `RecordImprovement` (validation in domain); seed import upserts row only (links not round-tripped — JSON column sufficient per planner lock). No spawn 06a/06b. **Re-run PASS:** `go test ./internal/domain/... ./internal/store/... ./internal/retrieval/... -count=1 -run 'TestRecordPredictedImpact\|TestImpactCompare\|TestRegression\|TestSetAttributionCaused\|TestRecordImprovement\|TestSeedExportIncludesImprovements'` → ok domain 0.580s ok store 0.106s; `go test ./internal/domain/... -count=30 -run TestRegressionDetectedVsPriorPassingTest` → ok 1.131s (30/30); `CGO_ENABLED=1 go test ./evals/compat/... -count=1 -run TestCompatibilitySecurityChecklist` → ok 1.026s; `CGO_ENABLED=1 go test ./cmd/trace/... -count=1 -run 'TestImpact\|TestOutcomes'` → ok 0.238s; `go test ./internal/mcp/... -count=1 -run TestToolNamesRegistered` → ok 0.005s; `go test ./internal/retrieval/... -count=1 -run TestImpactWalkIncludesAffectedTests` → ok 0.037s; `ls internal/store/schema/*.sql \| wc -l` → **24**. **Next `P22-S05-00`**. |
| 355 | P22-S05-00 | done | [phases/phase-22-capability-completion/scopes/scope-05-query-history/00-PLANNER.md](../phases/phase-22-capability-completion/scopes/scope-05-query-history/00-PLANNER.md) | 2026-08-18: scope planner **done**. Live: schema max **24** (24 sql); compat **24**; FTS+`Engine.Search` compiler-only; MCP **10**; `trace changes capture\|compare` only; no search/tests verifying/outcomes failed/regressions CLI; packet lacks evaluations/reflections/planning_evidence. Locked: no SQL; compat stays **24**; `trace search`; changes **list\|show**; evidence CLI four queries; MCP **trace_search**+**trace_changes** (S05-01→12), **trace_regressions** (S05-03→13); context cap 8×3 sections. Thickened 01–06. **No product Go**. **Next `P22-S05-01`**. |
| 356 | P22-S05-01 | done | [phases/phase-22-capability-completion/scopes/scope-05-query-history/01-search-changes.md](../phases/phase-22-capability-completion/scopes/scope-05-query-history/01-search-changes.md) | 2026-08-18: implementer **done**. C29/C30/C37 **true** (checklist **unboxed** — S05-02 review closes). No 025+ (schema max **24**); compat stays **24**. `trace search` + `changes list\|show` CLI; `ListChangesRecent` store query `created_at DESC, id DESC`; MCP **12** tools (`trace_search`, `trace_changes` after `trace_version`); `cli:search` + `cli:changes` gating. **PASS:** `CGO_ENABLED=1 go test ./cmd/trace/... ./internal/mcp/... -count=1 -run 'TestCLISearch\|TestCLIChanges\|TestMCPSearch\|TestMCPChanges\|TestToolNamesRegistered\|TestChangesCompare'` → ok cmd/trace 0.216s ok mcp 0.103s; `CGO_ENABLED=1 go test ./evals/compat/... -count=1 -run TestCompatibilitySecurityChecklist` → ok 0.941s; `ls internal/store/schema/*.sql \| wc -l` → **24**. Files: `cmd/trace/search.go`+test, `changes.go`+test, `root.go`, `help.go`, `internal/store/changes.go`, `internal/domain/changes.go`, `capability.go`+test, `internal/mcp/server.go`, `tools_search.go`, `tools_changes.go`, `export_test.go`, `mcp_test.go`, `cmd/trace-mcp/main.go`. **Next `P22-S05-02`**. |
| 357 | P22-S05-02 | done | [phases/phase-22-capability-completion/scopes/scope-05-query-history/02-search-changes-review.md](../phases/phase-22-capability-completion/scopes/scope-05-query-history/02-search-changes-review.md) | 2026-08-18: independent review **done**. Confidence **medium** (C37 blocker). **C30 closed** (checklist unboxed): `trace changes list|show` newest-first bounded (`ListChangesRecent` `created_at DESC, id DESC`; default 32 cap 64); show paths-only no blobs (`TestCLIChangesShow`); compare unchanged S02 (`TestChangesCompare` PASS). **C29 FAIL (high):** search CLI+MCP wired (`retrieval.Engine.Search`, G19 no MCP SQL) but **incremental FTS sync missing** on `UpsertChange` — `CreateChange`/`PromoteVCSCommitToChange` rows invisible to `trace search` until `RebuildFTS` or manual `SyncEntityFTS`; same gap on `UpsertRegression`/`UpsertOutcomeResult`/`UpsertReflection` (store/doc.go claims Upsert* sync; only goals/tasks/causal entities actually call `SyncEntityFTS`). **C37 FAIL (high):** `TestCLISearchUsesFTS`/`TestMCPSearchRegistered` **manually** call `SyncEntityFTS("change", …)` — proves plumbing only; independent probe: CreateChange without sync → **0 hits**. Historical evidence search not live for normal write paths. **Hold:** MCP catalog **12** (`TestToolNamesRegistered` stable order after `trace_version`); schema **24**; compat **24** PASS; limits 32/64 search+list; `cli:search`/`cli:changes` list/show/compare gated (`failCLIDenied`); S02 keepers PASS. **low** accepted: no named failCLIDenied test for search (wired like other CLI). **Spawn:** `P22-S05-02a` (Upsert* → `SyncEntityFTS` for change/regression/outcome_result/reflection + drop manual test sync + keeper) + `P22-S05-02b` (re-review + box C29/C30/C37). Checklist **unboxed**. **Re-run PASS:** `CGO_ENABLED=1 go test ./cmd/trace/... ./internal/mcp/... -count=1 -run 'TestCLISearch\|TestCLIChanges\|TestMCPSearch\|TestMCPChanges\|TestToolNamesRegistered\|TestChangesCompare\|TestChangesCapture'` → ok cmd/trace 0.354s ok mcp 0.086s; compat → ok 0.960s; schema sql **24**. **Next `P22-S05-02a`**. |
| 357a | P22-S05-02a | done | *(spawn — FTS upsert sync)* | 2026-08-18: implementer **done**. Incremental FTS sync on `UpsertChange`/`UpsertRegression`/`UpsertOutcomeResult`/`UpsertReflection` (mirror goals/tasks). Removed manual `SyncEntityFTS` from `TestCLISearchUsesFTS`/`TestMCPSearchRegistered`; added `TestSyncEntityFTSChange` keeper. No 025+ (schema **24**); compat stays **24**. **PASS:** `go test ./internal/store/... -count=1 -run 'TestSyncEntityFTSChange|TestChangeStoreRoundtrip|TestFTSFindsEntityTitleAndPathSymbol'` → ok 0.115s; `CGO_ENABLED=1 go test ./cmd/trace/... ./internal/mcp/... -count=1 -run 'TestCLISearch|TestMCPSearch|TestCLIChanges|TestMCPChanges|TestToolNamesRegistered|TestChangesCompare|TestChangesCapture'` → ok cmd/trace 0.360s ok mcp 0.089s; compat → ok 0.935s. Files: `changes.go`, `regressions.go`, `outcomes.go`, `fts_test.go`, `search_test.go`, `mcp_test.go`. **Next `P22-S05-02b`**. |
| 357b | P22-S05-02b | done | *(spawn — re-review search/changes)* | 2026-08-18: independent re-review **done**. Confidence **high**. **C29/C30/C37 closed** (checklist boxed). **C29 hold:** `trace search` + `trace changes list|show` answer history without extending `trace why`; `CreateChange`/`PromoteVCSCommitToChange` → `UpsertChange` → `SyncEntityFTS("change", …)` — searchable without manual sync (`TestSyncEntityFTSChange`, `TestCLISearchUsesFTS`, `TestMCPSearchRegistered` — zero manual `SyncEntityFTS` in tests). **C30 hold:** `ListChangesRecent` `created_at DESC, id DESC`; default 32 cap 64; show paths-only (`TestCLIChangesShow`); compare S02 (`TestChangesCompare` PASS). **C37 hold:** FTS incremental on `UpsertChange`/`UpsertRegression`/`UpsertOutcomeResult`/`UpsertReflection` (mirror goals/tasks); `loadEntityText` registers all four; CLI/MCP search via `retrieval.Engine.Search` (G19 — no MCP SQL). MCP catalog **12**; schema **24**; compat **24** PASS; S02 keepers PASS. **low** accepted: no named keeper for regression/outcome/reflection upsert FTS (change keeper + identical upsert pattern sufficient); no named `failCLIDenied` for search. No spawn 02c/02d. **Re-run PASS:** `go test ./internal/store/... -count=1 -run 'TestSyncEntityFTSChange|TestChangeStoreRoundtrip|TestFTSFindsEntityTitleAndPathSymbol'` → ok 0.106s; `CGO_ENABLED=1 go test ./cmd/trace/... ./internal/mcp/... -count=1 -run 'TestCLISearch|TestCLIChanges|TestMCPSearch|TestMCPChanges|TestToolNamesRegistered|TestChangesCompare|TestChangesCapture'` → ok cmd/trace 0.358s ok mcp 0.096s; compat → ok 0.937s; schema sql **24**. **Next `P22-S05-03`**. |
| 358 | P22-S05-03 | done | [phases/phase-22-capability-completion/scopes/scope-05-query-history/03-evidence-queries.md](../phases/phase-22-capability-completion/scopes/scope-05-query-history/03-evidence-queries.md) | 2026-08-18: implementer **done**. C17/C31–C34 **true** (checklist **unboxed** — S05-04 review closes). No 025+ (schema max **24**); compat stays **24**. Domain `queries.go`: `ListTestsValidatingSymbol`/`File`, `ListFailedOutcomes`, `ListWorkedApproaches`, `ListRegressions`; store `ListFailedTestOutcomes`/`ListPassingTestOutcomes`/`ListRegressionsRecent`. CLI `trace tests verifying`, `trace outcomes failed|worked`, `trace regressions list`; `cli:tests`/`cli:outcomes`/`cli:regressions`/`cli:tests` gated. MCP **13** (`trace_regressions` list only). **PASS:** `go test ./internal/domain/... -count=1 -run 'TestTestsVerifying|TestOutcomesFailed|TestRegressionsList|TestListRegressionsByChange'` → ok 0.163s; `CGO_ENABLED=1 go test ./cmd/trace/... ./internal/mcp/... -count=1 -run 'TestTestsVerifying|TestOutcomes|TestRegressions|TestMCPRegressions|TestToolNamesRegistered|TestHelpIncludesEvidence'` → ok cmd/trace 0.294s ok mcp 0.049s; compat → ok 0.939s; schema sql **24**. Files: `internal/domain/queries.go`+test, `internal/store/outcomes.go`, `internal/store/regressions.go`, `cmd/trace/{tests,test,outcomes,regressions}.go`, `evidence_queries_test.go`, `internal/mcp/tools_regressions.go`, `server.go`, `export_test.go`, `mcp_test.go`, `capability.go`+test, `help.go`, `root.go`. **Residuals for S05-04:** file-path verifying fail-closed (`TestTestsVerifyingCLI` missing.go); project-wide failed/worked bounded newest-first; C33 vs S06 successful-approaches overlap. **Next `P22-S05-04`**. |
| 359 | P22-S05-04 | done | [phases/phase-22-capability-completion/scopes/scope-05-query-history/04-evidence-queries-review.md](../phases/phase-22-capability-completion/scopes/scope-05-query-history/04-evidence-queries-review.md) | 2026-08-18: independent review **done**. Confidence **high**. **C17/C31–C34 closed** (checklist boxed). **C31:** `ListTestsValidatingSymbol`/`File` → store `ListValidatesForSymbol`/`ForFile` (zero grep/regex on filenames); `TestTestsVerifyingQuery` + `TestTestsVerifyingCLI` + S01 keeper `TestValidatesEdgeExtractedFromImport` PASS; unknown symbol/file fail-closed. **C32:** `ListFailedOutcomes` → `ListFailedTestOutcomes` SQL on `outcome_results` (`kind=test`, `test_status IN (fail,error)`); no subprocess; `TestOutcomesFailedAndWorked` + `TestOutcomesFailedCLI` PASS. **C33:** `ListWorkedApproaches` unions improvements + passing test rows; dedupe by id; newest-first + limit 32/64; `TestOutcomesFailedAndWorked` + `TestOutcomesWorkedCLI` PASS. **C17/C34:** `trace regressions list` + MCP `trace_regressions` action=list via `domain.ListRegressions`; change filter → `ListRegressionsByChangeID` join on `regression_associated_change` (no invented columns); S04 keeper `TestListRegressionsByChangeID` PASS; `TestRegressionsListQueryable` + `TestRegressionsListCLI` + `TestMCPRegressionsRegistered` PASS. **G19:** SQL in store/domain only; MCP/CLI thin encode. **Limits:** default 32 cap 64 on failed/regressions SQL; worked output clamped (project-wide merge loads all rows before sort — **low** accepted v1). **MCP catalog 13** stable order; S05-01 `trace_search`/`trace_changes` keepers PASS. Schema **24**; compat **24** PASS. **low** accepted: `trace tests verifying` gates `cli:test` not `cli:tests` (both AUTO_ALLOW builtins); no named limit-cap unit test for evidence queries; C33 vs S06 C23 overlap deferred S06. No spawn 04a/04b. **Re-run PASS:** `go test ./internal/domain/... ./internal/analyzers/... -count=1 -run 'TestTestsVerifying\|TestOutcomesFailed\|TestRegressionsList\|TestListRegressionsByChange\|TestValidatesEdgeExtractedFromImport'` → ok domain 0.230s ok analyzers 0.126s; `CGO_ENABLED=1 go test ./cmd/trace/... ./internal/mcp/... -count=1 -run 'TestTestsVerifying\|TestOutcomes\|TestRegressions\|TestMCPRegressions\|TestToolNamesRegistered\|TestHelpIncludesEvidence'` → ok cmd/trace 0.403s ok mcp 0.075s; S05-01 keepers `TestCLISearch\|TestCLIChanges\|TestMCPSearch\|TestMCPChanges\|TestChangesCompare\|TestChangesCapture` PASS; compat → ok 1.090s; schema sql **24**. **Next `P22-S05-05`**. |
| 360 | P22-S05-05 | done | [phases/phase-22-capability-completion/scopes/scope-05-query-history/05-context-evidence.md](../phases/phase-22-capability-completion/scopes/scope-05-query-history/05-context-evidence.md) | 2026-08-18: implementer **done**. C35/C42-surface **true** (checklist **unboxed** — S05-06 review closes). No 025+ (schema max **24**); compat stays **24**. `BuildEvidenceSections` cap **8** per array; task-scoped `evaluations`/`reflections`/`planning_evidence` on `compiler.Packet` (additive `schema_version` `"0.2"`); loop `planning_evidence` section mirrors three arrays + `freshness`; MD labeled sections; MCP `trace_context` inherits via compiler (G19). **PASS:** `go test ./internal/compiler/... ./internal/loop/... -count=1 -run 'TestContextIncludesEvaluations|TestLoopNextPlanningEvidence|TestTaskContextAndBudgets'` → ok compiler 0.099s ok loop 0.056s; `CGO_ENABLED=1 go test ./cmd/trace -count=1 -run 'TestContext|TestLoopNext'` → ok 0.719s; compat → ok 0.950s; schema sql **24**. Files: `evidence_sections.go`+test, `packet.go`, `compiler.go`, `next.go`, `next_test.go`. **Residuals for S05-06:** evidence outside items[] budget; empty arrays always emitted; cross-task filter review; cap-8 ordering tie-break. **Next `P22-S05-06`**. |
| 361 | P22-S05-06 | done | [phases/phase-22-capability-completion/scopes/scope-05-query-history/06-context-evidence-review.md](../phases/phase-22-capability-completion/scopes/scope-05-query-history/06-context-evidence-review.md) | 2026-08-18: independent review **done**. Confidence **high**. **S05 complete** — C17, C29–C35, C37 **closed**; **C42-surface closed** (checklist C35 boxed; full C42 line stays unboxed for S07 library). **C35:** `BuildEvidenceSections` cap **8** per array; task-scoped `evaluations`/`reflections`/`planning_evidence` on `compiler.Packet` (`schema_version` `"0.2"`); cross-task filter asserted (`TestContextIncludesEvaluationsAndReflections`); `scores_json` truncate 512; reflections summary-only (no assumption blobs). **Loop:** `planning_evidence` section mirrors three arrays + `freshness`; `trace.loop.next.v1` unchanged (`TestLoopNextPlanningEvidenceSection`). **MCP:** `trace_context` → `compiler.TaskContext`/`ExpandContext` — no fork (G19). **C29/C30/C37 hold:** S05-02b — FTS incremental on Upsert*; search/changes CLI+MCP keepers PASS. **C17/C31–C34 hold:** S05-04 — evidence queries + `trace_regressions` MCP. MCP catalog **13**; schema **24** sql; compat **24** PASS. S01–S04 keepers PASS (`TestImpactWalkIncludesAffectedTests`, `TestChangesCompare`, `TestRegressionDetectedVsPriorPassingTest` 30/30, `TestRecordImprovement`). No **025+**; no daemon/HTTP. **low** accepted: no named cap-8 overflow test; MCP parity test asserts task_id/schema only (compiler path sufficient); empty evidence arrays always emitted `[]`; C42 checklist line deferred S07-05. No spawn 06a/06b. **Re-run PASS:** `go test ./internal/compiler/... ./internal/loop/... -count=1 -run 'TestContextIncludesEvaluations|TestLoopNextPlanningEvidence|TestTaskContextAndBudgets'` → ok compiler 0.124s ok loop 0.073s; `CGO_ENABLED=1 go test ./cmd/trace/... ./internal/mcp/... -count=1 -run 'TestCLISearch|TestCLIChanges|TestTestsVerifying|TestOutcomes|TestRegressions|TestContext|TestLoopNext|TestToolNamesRegistered'` → ok cmd/trace 1.481s ok mcp 0.007s; `go test ./internal/domain/... -count=30 -run TestRegressionDetectedVsPriorPassingTest` → ok 1.075s; compat → ok 0.965s; schema sql **24**. **Next `P22-S06-00`**. |
| 362 | P22-S06-00 | done | [phases/phase-22-capability-completion/scopes/scope-06-learning-knowledge/00-PLANNER.md](../phases/phase-22-capability-completion/scopes/scope-06-learning-knowledge/00-PLANNER.md) | 2026-08-18: scope planner **done**. Live: schema max **24** (24 sql); compat **24**; MCP **13**; changes/effects/outcomes/regressions/improvements/reconsiderations/reflections live; S05 evidence+context cap **8**; no `change_patterns`/`engineering_knowledge`/patterns.go/knowledge.go; no `trace patterns|knowledge|changes similar`. Locked: mig **025** both tables (**S06-01** owns mig+patterns; **S06-03** knowledge); compat **25** after S06-01; `InferChangeKind`=`seg:<segment>`; outcome priority regression→…→neutral; tend threshold **2**; `QuerySimilarChanges` path\|kind; synthesize from reconsiderations/reflections/patterns/improvements; seed keys `change_patterns`+`engineering_knowledge`; packet tendencies+successful_approaches+optional similar_changes cap **8**; MCP stays **13**; no ML (D-22-11). Thickened 01–06. **No product Go**. **Next `P22-S06-01`**. |
| 363 | P22-S06-01 | done | [phases/phase-22-capability-completion/scopes/scope-06-learning-knowledge/01-patterns-similar.md](../phases/phase-22-capability-completion/scopes/scope-06-learning-knowledge/01-patterns-similar.md) | 2026-08-18: implementer **done**. C19/C20 **true** (checklist **unboxed** — S06-02 review closes). Mig **025** `change_patterns`+`engineering_knowledge`; compat/embed **25** (no 026+). `InferChangeKind`/`ClassifyChangeOutcome` pure + priority tests; `RefreshChangePatterns` full rebuild; `QuerySimilarChanges` path\|kind mutually exclusive fail-closed; excludes SUPERSEDED only. CLI `trace patterns refresh\|list`; `trace changes similar --path\|--kind`; `cli:patterns` AUTO_ALLOW. MCP catalog **13** unchanged. **PASS:** `go test ./internal/domain/... ./internal/store/... -count=1 -run 'TestPatternCountsFromChangesAndOutcomes\|TestQuerySimilarChanges\|TestQuerySimilarChangesFailClosed\|TestOpenCreatesDBAndMigratesIdempotent\|TestMigrationStatusReportsEmbedMax'` → ok domain 0.135s ok store 0.113s; `CGO_ENABLED=1 go test ./cmd/trace/... -count=1 -run 'TestPatterns\|TestChangesSimilar\|TestChangesCompare'` → ok 0.202s; `CGO_ENABLED=1 go test ./evals/compat/... -count=1 -run TestCompatibilitySecurityChecklist` → ok 1.004s; `ls internal/store/schema/*.sql \| wc -l` → **25**. Files: `025_engineering_knowledge.sql`, `internal/domain/patterns.go`+test, `internal/store/patterns.go`+test, `cmd/trace/patterns.go`+test, `changes.go`+test, `root.go`, `help.go`, `capability.go`, compat/store embed bumps. **Residuals for S06-02:** `ClassifyChangeOutcome` priority fixture regression+improvement same change; path-prefix vs kind `InferChangeKind` consistency; VCS-capture in patterns; empty patterns list before refresh. **Next `P22-S06-02`**. |
| 364 | P22-S06-02 | done | [phases/phase-22-capability-completion/scopes/scope-06-learning-knowledge/02-patterns-similar-review.md](../phases/phase-22-capability-completion/scopes/scope-06-learning-knowledge/02-patterns-similar-review.md) | 2026-08-18: independent review **done**. Confidence **medium** (C20 blocker). **C19 hold:** `RefreshChangePatterns` aggregates `(change_kind, outcome_kind)` pos/neg counts; `TestPatternCountsFromChangesAndOutcomes` PASS; `InferChangeKind` lex-smallest path → `seg:<segment>` (`TestInferChangeKind`); `ClassifyChangeOutcome` priority incl. regression+improvement same change (`TestClassifyChangeOutcomePriority`); empty `patterns list` → `[]` (`TestPatternsListEmptyBeforeRefresh`). **C20 FAIL (high):** `TestQuerySimilarChanges` **FAIL every `-count=1`** — rapid `CreateChange` shares second-precision `created_at`; newest-first tie-break uses UUID `id DESC` not insertion order (same class as P22-S03-06a). `listChangesByKind` in-memory + `ListChangesByPathPrefix` SQL both affected. CLI `TestChangesSimilarCLI` PASS (single internal row — no ordering assert). **No ML:** grep patterns paths clean. Schema **25** sql; **`025_engineering_knowledge.sql`** both tables; compat/embed **25** PASS. MCP **13** unchanged (G19). Limits 32/64; similar JSON paths-only (`SimilarChangeRow`). S05 keepers `TestCLIChangesList`, `TestChangesCompare`, `TestSyncEntityFTSChange` PASS. Checklist **unboxed**. **Spawn:** `P22-S06-02a` (ordering fix) + `P22-S06-02b` (re-review + box). **Re-run:** domain/store pattern tests → `TestQuerySimilarChanges` FAIL; CLI patterns/similar → ok; compat → ok; MCP → ok; schema **25**. **Next `P22-S06-02a`**. |
| 364a | P22-S06-02a | done | [phases/phase-22-capability-completion/scopes/scope-06-learning-knowledge/02a-similar-ordering-fix.md](../phases/phase-22-capability-completion/scopes/scope-06-learning-knowledge/02a-similar-ordering-fix.md) | 2026-08-18: implementer **done**. C20 ordering fix (checklist **unboxed** — S06-02b closes). No 026+ (schema **25**); compat stays **25**. `ListChangesByPathPrefix` + `ListActiveChanges` tie-break `ORDER BY created_at DESC/ASC, rowid DESC/ASC`; `listChangesByKind` sorts by `RowID` not UUID; `Change.RowID` loaded from store. **PASS:** `go test ./internal/domain/... -count=30 -run TestQuerySimilarChanges$` → `ok 1.051s` (30/30 stable); `go test ./internal/domain/... -count=1 -run 'TestQuerySimilarChangesSameSecondTimestamps\|TestQuerySimilarChanges\|TestPatternCountsFromChangesAndOutcomes\|TestQuerySimilarChangesFailClosed'` → ok; `go test ./internal/domain/... ./internal/store/... -count=1 -run 'TestQuerySimilarChanges\|TestPatternCountsFromChangesAndOutcomes\|TestOpenCreatesDBAndMigratesIdempotent\|TestMigrationStatusReportsEmbedMax'` → ok domain/store; `CGO_ENABLED=1 go test ./cmd/trace/... -count=1 -run 'TestPatterns\|TestChangesSimilar\|TestChangesCompare\|TestCLIChanges'` → ok. Files: `internal/store/changes.go`, `internal/store/patterns.go`, `internal/domain/patterns.go`, `internal/domain/patterns_test.go`. **Next `P22-S06-02b`**. |
| 364b | P22-S06-02b | done | [phases/phase-22-capability-completion/scopes/scope-06-learning-knowledge/02b-similar-ordering-fix-review.md](../phases/phase-22-capability-completion/scopes/scope-06-learning-knowledge/02b-similar-ordering-fix-review.md) | 2026-08-18: independent re-review **done**. Confidence **high**. **C19/C20 closed** (checklist boxed). **C20:** `ListChangesByPathPrefix` + `ListActiveChanges` tie-break `ORDER BY created_at DESC/ASC, rowid DESC/ASC`; `listChangesByKind` sorts by `RowID` not UUID; `Change.RowID` loaded via `scanChanges`. `TestQuerySimilarChanges` **30/30** stable; `TestQuerySimilarChangesSameSecondTimestamps` PASS (UUID-lex inverted ids, insertion order wins). **C19 hold:** `RefreshChangePatterns` deterministic; `TestPatternCountsFromChangesAndOutcomes` PASS; `TestInferChangeKind`/`TestClassifyChangeOutcomePriority`/`TestPatternsListEmptyBeforeRefresh` PASS. **No ML:** grep patterns paths clean (D-22-11). Schema **25** sql; compat/embed **25** PASS. MCP catalog **13** unchanged. Similar JSON paths-only; limits 32/64; S05 keepers `TestCLIChanges`/`TestChangesCompare`/`TestSyncEntityFTSChange` PASS. No spawn 02c/02d. **Re-run PASS:** `go test ./internal/domain/... -count=30 -run TestQuerySimilarChanges$` → ok 1.104s; `go test ./internal/domain/... ./internal/store/... -count=1 -run 'TestPatternCountsFromChangesAndOutcomes\|TestQuerySimilarChanges\|TestOpenCreatesDBAndMigratesIdempotent\|TestMigrationStatusReportsEmbedMax'` → ok domain/store; `CGO_ENABLED=1 go test ./cmd/trace/... -count=1 -run 'TestPatterns\|TestChangesSimilar\|TestChangesCompare\|TestCLIChanges'` → ok; compat → ok 0.970s; MCP → ok 0.005s; schema sql **25**. **Next `P22-S06-03`**. |
| 365 | P22-S06-03 | done | [phases/phase-22-capability-completion/scopes/scope-06-learning-knowledge/03-project-knowledge.md](../phases/phase-22-capability-completion/scopes/scope-06-learning-knowledge/03-project-knowledge.md) | 2026-08-18: implementer **done**. C10/C21/C26/C27 **true** (checklist **unboxed** — S06-04 review closes). No 026+ (schema max **25**); compat stays **25**. `UpsertEngineeringKnowledge`/`GetEngineeringKnowledge`/`ListEngineeringKnowledge`; `SynthesizeKnowledge` calls `RefreshChangePatterns` then upserts from reconsiderations→`decision`, reflections→`reflection`, patterns (count≥2)→`pattern`, improvements→`improvement`; stable ids `ek:<source_type>:<source_entity_id>`; reconsideration `body_json.decision_id`. Seed `change_patterns[]`+`engineering_knowledge[]` export/import stable ids. CLI `trace knowledge list [--topic] [--limit N]` + `trace knowledge synthesize` JSON `{ok,created,updated}`; `cli:knowledge` AUTO_ALLOW. MCP catalog **13** unchanged. **PASS:** `go test ./internal/domain/... -count=1 -run 'TestUpsertEngineeringKnowledge\|TestSynthesizeKnowledgeFromPatterns\|TestKnowledgeLinksDecision\|TestSeedExportIncludesKnowledge\|TestSeedExportIncludesImprovements'` → ok 1.393s; `CGO_ENABLED=1 go test ./cmd/trace/... -count=1 -run 'TestKnowledge'` → ok 0.063s; `CGO_ENABLED=1 go test ./evals/compat/... -count=1 -run TestCompatibilitySecurityChecklist` → ok 0.972s; `ls internal/store/schema/*.sql \| wc -l` → **25**. Files: `internal/domain/knowledge.go`+test, `internal/store/knowledge.go`, `seed_export.go`/`seed_import.go`, `store/patterns.go` (`UpsertChangePattern`, `ListAllChangePatterns`), `cmd/trace/knowledge.go`+test, `root.go`, `help.go`, `capability.go`. **Residuals for S06-04:** superseded status never set in v1 synthesize (active only); pattern/improvement synthesize skips empty summary; seed import upserts rows only (links via JSON). **Next `P22-S06-04`**. |
| 366 | P22-S06-04 | done | [phases/phase-22-capability-completion/scopes/scope-06-learning-knowledge/04-project-knowledge-review.md](../phases/phase-22-capability-completion/scopes/scope-06-learning-knowledge/04-project-knowledge-review.md) | 2026-08-18: independent review **done**. Confidence **high**. **C10/C21/C26/C27 closed** (checklist boxed). **C10:** `SynthesizeKnowledge` upserts reconsiderations with `body_json.decision_id`; `TestKnowledgeLinksDecision` PASS. **C21:** synthesize from live DB sources (reconsiderations→decision, reflections→reflection, patterns count≥2→pattern, improvements→improvement) after `RefreshChangePatterns` — deterministic, no LLM. **C26:** `engineering_knowledge` `created_at`/`updated_at`; upsert ON CONFLICT preserves `created_at`, advances `updated_at`; idempotent stable ids `ek:<source_type>:<source_entity_id>`. **C27:** second `SynthesizeKnowledge` updates in place (`Created=0`, `Updated≥1`, row count stable, `updated_at` advances) — `TestSynthesizeKnowledgeFromPatterns` PASS. **Seed (W-26/D-22-19):** `change_patterns[]`+`engineering_knowledge[]` export/import stable ids; `TestSeedExportIncludesKnowledge`+`TestSeedExportIncludesImprovements` PASS. **No LLM (W-27):** grep knowledge paths clean. **Provenance:** `evidence_ids` validated via `GetEvidence`; row `source_type` per source. **Schema max 25** (25 sql, no 026+); compat/embed **25** PASS. **S06-01 hold:** `TestPatternCountsFromChangesAndOutcomes`+`TestQuerySimilarChanges` PASS. **G19:** CLI thin (`domain.SynthesizeKnowledge`); MCP catalog **13** unchanged. **No blobs:** `body_json` structured metadata only. **low** accepted: no named assertion for reflection-topic rows (full synthesize on `seedP20Cognition` includes reflection path); v1 never sets `superseded`; `ListChangePatterns(64)` cap during synthesize; seed import upserts rows only (links via JSON). No spawn 04a/04b. **Re-run PASS:** `go test ./internal/domain/... -count=1 -run 'TestUpsertEngineeringKnowledge\|TestSynthesizeKnowledgeFromPatterns\|TestKnowledgeLinksDecision\|TestSeedExportIncludesKnowledge\|TestSeedExportIncludesImprovements\|TestPatternCountsFromChangesAndOutcomes'` → ok 1.446s; `CGO_ENABLED=1 go test ./cmd/trace/... -count=1 -run 'TestKnowledge\|TestPatterns'` → ok 0.156s; `CGO_ENABLED=1 go test ./evals/compat/... -count=1 -run TestCompatibilitySecurityChecklist` → ok 1.012s; `go test ./internal/mcp/... -count=1 -run TestToolNamesRegistered` → ok 0.009s; schema sql **25**. **Next `P22-S06-05`**. |
| 367 | P22-S06-05 | done | [phases/phase-22-capability-completion/scopes/scope-06-learning-knowledge/05-tend-help-hurt.md](../phases/phase-22-capability-completion/scopes/scope-06-learning-knowledge/05-tend-help-hurt.md) | 2026-08-18: implementer **done**. C22/C23/C24 **true** (checklist **unboxed** — S06-06 review closes). No 026+ (schema max **25**); compat stays **25**. `ListTendencies` threshold **2** (`direction=improve\|damage`); `ListSuccessfulApproaches` merges `ListWorkedApproaches` + active knowledge (`topic∈{improvement,pattern}` improve-only); packet `tendencies[]`+`successful_approaches[]` cap **8** (`schema_version` `"0.2"`); loop sections `tendencies`/`successful_approaches`/`similar_changes` (optional, kind from latest change); CLI `trace knowledge tendencies [--limit N]`; MCP **13** via `trace_context`. S05 evidence sections unchanged. **PASS:** `go test ./internal/compiler/... ./internal/loop/... -count=1 -run 'TestTendHelpHurtInContext\|TestSuccessfulApproachesSurfaced\|TestLoopNextIncludesEvidenceForDecisions\|TestContextIncludesEvaluations\|TestLoopNextPlanningEvidence'` → ok compiler 0.151s ok loop 0.118s; `CGO_ENABLED=1 go test ./cmd/trace -count=1 -run 'TestContext\|TestLoopNext\|TestKnowledgeTendencies\|TestOutcomesWorked'` → ok 0.839s; compat → ok 0.938s; schema sql **25**. Files: `patterns.go`, `knowledge.go`, `evidence_sections.go`+test, `packet.go`, `next.go`+test, `knowledge.go`+test (cmd), `help.go`. **Next `P22-S06-06`**. |
| 368 | P22-S06-06 | done | [phases/phase-22-capability-completion/scopes/scope-06-learning-knowledge/06-tend-help-hurt-review.md](../phases/phase-22-capability-completion/scopes/scope-06-learning-knowledge/06-tend-help-hurt-review.md) | 2026-08-18: independent review **done**. Confidence **high**. **S06 complete** — C10, C19–C24, C26, C27 **closed** (checklist all nine S06-owned bullets `[x]`). **C22:** `ListTendencies` threshold **≥2** (`direction=improve\|damage`); packet+loop `tendencies[]` via `buildTendencies`/`TendenciesSection`; CLI `trace knowledge tendencies`; `TestTendHelpHurtInContext` PASS. **C23:** `ListSuccessfulApproaches` merges `ListWorkedApproaches` + active knowledge (`topic∈{improvement,pattern}` improve-only); dedupe by id; packet+loop cap **8**; `TestSuccessfulApproachesSurfaced` + S05 `TestOutcomesWorkedCLI` PASS. **C24:** loop next combines `tendencies`+`successful_approaches`+`planning_evidence` (evaluations/reflections/planning_evidence); optional `similar_changes` cap **8**; `TestLoopNextIncludesEvidenceForDecisions` PASS. **Caps:** `evidenceSectionCap=8` for tendencies/successful_approaches/planning_evidence; `buildSimilarChangesSection` limit **8**. **C19/C20 hold:** S06-02b — `TestPatternCountsFromChangesAndOutcomes`+`TestQuerySimilarChanges` **30/30** stable. **C10/C21/C26/C27 hold:** S06-04 — synthesize+seed keepers PASS. **W-27 no ML:** grep S06 product paths clean. **Schema max 25** (25 sql, no 026+); compat/embed **25** PASS. **MCP catalog 13** unchanged (`trace_context` inherits compiler). S01–S05 spot-check PASS (`TestImpactWalkIncludesAffectedTests`, `TestRegressionDetectedVsPriorPassingTest` 30/30, `TestCLIChanges`, `TestSyncEntityFTSChange`). **low** accepted: tendencies/successful_approaches project-wide in packet (planner lock); no named cap-8 overflow keeper for tendencies (constant shared with S05 evidence); `similar_changes` empty when task has no changes (optional section). No spawn 06a/06b. **Re-run PASS:** `go test ./internal/compiler/... ./internal/loop/... ./internal/domain/... -count=1 -run 'TestTendHelpHurtInContext\|TestSuccessfulApproachesSurfaced\|TestLoopNextIncludesEvidenceForDecisions\|TestPatternCountsFromChangesAndOutcomes\|TestQuerySimilarChanges\|TestSynthesizeKnowledgeFromPatterns\|TestKnowledgeLinksDecision\|TestSeedExportIncludesKnowledge'` → ok compiler/loop/domain; `CGO_ENABLED=1 go test ./cmd/trace/... -count=1 -run 'TestContext\|TestLoopNext\|TestKnowledge\|TestPatterns\|TestChangesSimilar\|TestOutcomesWorked\|TestCLIChanges'` → ok; `go test ./internal/domain/... -count=30 -run TestRegressionDetectedVsPriorPassingTest` → ok; compat+MCP+schema **25** PASS. **Next `P22-S07-00`**. |
| 369 | P22-S07-00 | done | [phases/phase-22-capability-completion/scopes/scope-07-extensible-eval/00-PLANNER.md](../phases/phase-22-capability-completion/scopes/scope-07-extensible-eval/00-PLANNER.md) | 2026-08-18: scope planner **done**. Live: schema max **25** (25 sql); compat **25**; MCP **13**; outcomes test/verification/evaluation only; `CheckArchitecturalInvariants` one default rule (S03); context evaluations cap 8 no mechanism_id (S05); no `internal/eval/`; no `trace/eval-rules.json`. Locked: package `internal/eval`; `Mechanism` contract; built-ins stored_test/stored_verification/stored_evaluation/architectural_invariant; mig **026** `eval_rule_sets`; compat **26** after S07-01; `trace/eval-rules.json` v1; `ListResults` maps outcomes→mechanism_id (no outcome schema change); seed `eval_rules_path` pointer (W-31). Thickened 01–06. **No product Go**. **Next `P22-S07-01`**. |
| 370 | P22-S07-01 | done | [phases/phase-22-capability-completion/scopes/scope-07-extensible-eval/01-evaluator-contract.md](../phases/phase-22-capability-completion/scopes/scope-07-extensible-eval/01-evaluator-contract.md) | 2026-08-18: implementer **done**. C40/C43 **true** (checklist **unboxed** — S07-02 review closes). Mig **026** `eval_rule_sets`; compat/embed **26** (no 027+). New `internal/eval` package: `Mechanism` contract, thread-safe registry, `RunAll` (stable id order; continue-on-fail; aggregate error only invalid task/service); four built-ins via `mechs/` → domain helpers (`HasTestOutcomeSinceLatestChange`, `CheckVerificationGate`/`HasVerificationOutcome`, `HasComputedEvaluation`, `CheckArchitecturalInvariants`); store `UpsertEvalRuleSet`/`GetEvalRuleSet` stub; no `CoordinateVerification`/CLI wiring; MCP catalog **13** unchanged; domain does not import eval. **PASS:** `go test ./internal/eval/... ./internal/store/... -count=1 -run 'TestEvalRegistry|TestAddMechanism|TestOpenCreatesDBAndMigratesIdempotent|TestMigrationStatusReportsEmbedMax'` → ok eval 0.037s ok store 0.078s; `CGO_ENABLED=1 go test ./evals/compat/... -count=1 -run TestCompatibilitySecurityChecklist` → ok 0.957s; `go test ./internal/domain/... -count=30 -run TestRegressionDetectedVsPriorPassingTest` → ok 1.102s (30/30); `ls internal/store/schema/*.sql \| wc -l` → **26**. Files: `026_eval_rules.sql`, `internal/eval/{types,registry,run,register_builtins,registry_test}.go`, `internal/eval/mechs/{types,storedtest,stored_verification,stored_evaluation,architectural_invariant}.go`, `internal/store/eval_rules.go`, compat/store embed bumps. **Next `P22-S07-02`**. |
| 371 | P22-S07-02 | done | [phases/phase-22-capability-completion/scopes/scope-07-extensible-eval/02-evaluator-contract-review.md](../phases/phase-22-capability-completion/scopes/scope-07-extensible-eval/02-evaluator-contract-review.md) | 2026-08-18: independent review **done**. Confidence **high**. **C40/C43 closed** (checklist boxed). **C40:** `DefaultRegistry` lists four built-ins (`stored_test`, `stored_verification`, `stored_evaluation`, `architectural_invariant`); `TestEvalRegistryMultipleMechanisms` PASS; `RunAll` stable id sort + filter unknown ids (`TestRunAllFiltersUnknownMechanismIDs`); continue-on-fail (`TestRunAllContinuesOnMechanismFailure`); each built-in delegates to domain helpers only — zero SQL in `internal/eval`. **C43:** `TestAddMechanismWithoutSchemaChange` — `fake_echo` registers+runs; schema **26** sql (`026_eval_rules.sql` only new mig); `outcome_results` kind CHECK unchanged; `CoordinateVerification`/`RecordEvaluationOutcome` unwired (grep: no `internal/eval` import outside eval package). **Landmines:** eval→domain only (no reverse import); no daemon/subprocess in eval; MCP catalog **13**; S03 keeper `TestRegressionDetectedVsPriorPassingTest` **30/30**; S03 keepers `TestCoordinateVerification`/`TestBuildPolicyInputs`/`TestInvariant` PASS. Compat/embed **26** PASS. **low** accepted: no named test asserts RunAll result slice sort order (implementation sorts via `selectMechanismIDs`); `fake_echo` persists in process registry after extensibility test. No spawn 02a/02b. **Re-run PASS:** `go test ./internal/eval/... ./internal/store/... -count=1 -run 'TestEvalRegistry|TestAddMechanism|TestOpenCreatesDBAndMigratesIdempotent|TestMigrationStatusReportsEmbedMax'` → ok eval/store; `CGO_ENABLED=1 go test ./evals/compat/... -count=1 -run TestCompatibilitySecurityChecklist` → ok; `go test ./internal/domain/... -count=30 -run TestRegressionDetectedVsPriorPassingTest` → ok; `go test ./internal/mcp/... -count=1 -run TestToolNamesRegistered` → ok; schema sql **26**. **Next `P22-S07-03`**. |
| 372 | P22-S07-03 | done | [phases/phase-22-capability-completion/scopes/scope-07-extensible-eval/03-project-eval-rules.md](../phases/phase-22-capability-completion/scopes/scope-07-extensible-eval/03-project-eval-rules.md) | 2026-08-18: implementer **done**. C41 **true** (checklist **unboxed** — S07-04 review closes). No 027+ (schema **26** sql); compat stays **26**. `internal/eval/rules.go`: `RulesPath`/`LoadRules`/`DefaultRules`; missing file → four built-ins + default invariant; invalid JSON/version fail-closed; unknown mechanism ids skipped (log); cache upsert on successful parse. `RunAll` loads rules from `Root`+`Store`; mechanism filter preserves file order; `architectural_invariant` wraps domain check via `rulectx` (disabled `internal_must_not_import_cmd` → Passed=true). CLI `trace eval rules` JSON; `cli:eval` AUTO_ALLOW. Seed export `eval_rules_path` when file on disk; import pointer-only (`body_json={}`). CONTRIBUTING paragraph. **PASS:** `go test ./internal/eval/... -count=1 -run 'TestProjectEvalRules|TestMissingEvalRules|TestEvalRegistry'` → ok 0.125s; `CGO_ENABLED=1 go test ./cmd/trace/... -count=1 -run 'TestEvalRules'` → ok 0.079s; `go test ./internal/domain/... -count=1 -run 'TestSeedExportIncludesEvalRules|TestInvariant'` → ok 0.164s; `CGO_ENABLED=1 go test ./evals/compat/... -count=1 -run TestCompatibilitySecurityChecklist` → ok 0.961s; schema sql **26**. **Next `P22-S07-04`**. |
| 373 | P22-S07-04 | done | [phases/phase-22-capability-completion/scopes/scope-07-extensible-eval/04-project-eval-rules-review.md](../phases/phase-22-capability-completion/scopes/scope-07-extensible-eval/04-project-eval-rules-review.md) | 2026-08-18: independent review **done**. Confidence **high**. **C41 closed** (checklist boxed). **C41:** `RulesPath`/`evalRulesRelPath` locked **`trace/eval-rules.json`** (not `.trace/`); `TestProjectEvalRulesLoaded` PASS (parse + `eval_rule_sets` cache upsert); `TestMissingEvalRulesUsesBuiltins` PASS (four built-ins when file absent); `TestProjectEvalRulesOverrideDefaultInvariant` PASS (domain still fails; mechanism passes with disabled `internal_must_not_import_cmd`); `TestLoadRulesInvalidJSONFailClosed`/`TestLoadRulesWrongVersionFailClosed` fail-closed via `domain.ErrValidation`; CLI `trace eval rules` JSON (`TestEvalRules`) PASS. **W-31:** `TestSeedExportIncludesEvalRulesPath`/`TestSeedExportOmitsEvalRulesPathWhenMissing` — pointer-only export/import (`body_json={}`); CONTRIBUTING paragraph documents path. **Holds:** C40/C43 keeper tests PASS; schema **26** sql (no 027+); compat/embed **26** PASS; `CheckArchitecturalInvariants` unchanged in domain — eval `architectural_invariant` wraps via `rulectx`; MCP catalog **13**; domain does not import eval; no repo-root `trace/eval-rules.json` committed (testdata only). **low:** fail-closed tests assert `err != nil` only (not typed `ErrValidation`). No spawn 04a/04b. **Re-run PASS:** `go test ./internal/eval/... -count=1 -run 'TestProjectEvalRules|TestMissingEvalRules|TestEvalRegistry|TestAddMechanism'` → ok; `CGO_ENABLED=1 go test ./cmd/trace/... -count=1 -run 'TestEvalRules'` → ok; `go test ./internal/domain/... -count=1 -run 'TestInvariant|TestSeedExportIncludesEvalRules'` → ok; `CGO_ENABLED=1 go test ./evals/compat/... -count=1 -run TestCompatibilitySecurityChecklist` → ok; `go test ./internal/mcp/... -count=1 -run TestToolNamesRegistered` → ok. **Next `P22-S07-05`**. |
| 374 | P22-S07-05 | done | [phases/phase-22-capability-completion/scopes/scope-07-extensible-eval/05-eval-results-available.md](../phases/phase-22-capability-completion/scopes/scope-07-extensible-eval/05-eval-results-available.md) | 2026-08-18: implementer **done**. **C42-library true** (checklist full C42 line stays **partially boxed** — S07-06 review closes surface+library). No 027+ (schema **26**); compat stays **26**. `eval.ListResults(ctx, svc, taskID)` + `ResultRow{mechanism_id,...}` maps stored outcomes test→`stored_test`, verification→`stored_verification`, evaluation→`stored_evaluation`; full `comparison_json` on evaluation rows; stored-outcomes-only (no RunAll synthesis in list); default limit **32** cap **64**; newest-first sort (`created_at DESC`, id tiebreak). Domain `ListEvalOutcomeResults` read via svc. CLI `trace eval results --task <id>` JSON; `cli:eval` reuse. S05 compiler untouched (`buildEvaluations` keeper PASS). MCP catalog **13** unchanged. **PASS:** `go test ./internal/eval/... -count=1 -run 'TestListEvaluationResultsForFutureAgents|TestEvalResultsIncludeMechanismID'` → ok 0.119s; `go test ./internal/compiler/... -count=1 -run TestContextIncludesEvaluations` → ok 0.063s; `CGO_ENABLED=1 go test ./cmd/trace/... -count=1 -run 'TestEvalResults|TestEvalRules'` → ok 0.264s; `CGO_ENABLED=1 go test ./evals/compat/... -count=1 -run TestCompatibilitySecurityChecklist` → ok 1.457s; schema sql **26**. Files: `internal/eval/query.go`+test, `internal/domain/outcomes.go` (`ListEvalOutcomeResults`), `cmd/trace/eval.go`+test, `help.go`. **Residuals for S07-06:** compiler still reads store directly (optional delegate); live RunAll vs stored list documented; full C42 checklist box at review. **Next `P22-S07-06`**. |
| 375 | P22-S07-06 | done | [phases/phase-22-capability-completion/scopes/scope-07-extensible-eval/06-eval-results-available-review.md](../phases/phase-22-capability-completion/scopes/scope-07-extensible-eval/06-eval-results-available-review.md) | 2026-08-18: independent review **done**. Confidence **high**. **S07 complete** — C40–C43 **closed** (checklist all four S07-owned bullets `[x]`). **C42:** surface (S05 hold) — `TestContextIncludesEvaluationsAndReflections` PASS via `-run TestContextIncludesEvaluations`; `evidenceSectionCap=8`; cross-task filter asserted; library — `TestListEvaluationResultsForFutureAgents` PASS (`eval.ListResults` round-trip, full `comparison_json`); `TestEvalResultsIncludeMechanismID` PASS (all rows `mechanism_id`, test/verification/evaluation mappings). **C40 hold:** four built-ins + `TestAddMechanismWithoutSchemaChange` fake extensibility (S07-02). **C41 hold:** `trace/eval-rules.json` load/override + seed pointer (S07-04). **C43 hold:** outcome kind enum unchanged; schema **26** sql; compat/embed **26**; zero `os/exec`/daemon in `internal/eval`. Spot-check keepers PASS: `TestImpactWalkIncludesAffectedTests`; `TestRegressionDetectedVsPriorPassingTest` **30/30**; `TestQuerySimilarChanges`; MCP catalog **13**; no **027+** migrations. CLI `trace eval results`/`rules` PASS. **low** accepted (carry-forward from S07-05): compiler `buildEvaluations` still reads store directly (optional future delegate to `eval.ListResults`); `ListResults` stored-only vs `RunAll` live checks documented. No spawn 06a/06b. **Re-run PASS:** eval/registry/rules/list tests → ok 0.280s; compiler/loop context → ok; domain regression 30/30 + keepers → ok; `CGO_ENABLED=1` cmd/trace eval+context → ok 0.250s; compat **26** → ok 1.092s; schema sql **26**. **Next `P22-S08-00`**. |
| 376 | P22-S08-00 | done | [phases/phase-22-capability-completion/scopes/scope-08-agent-workflow-verify/00-PLANNER.md](../phases/phase-22-capability-completion/scopes/scope-08-agent-workflow-verify/00-PLANNER.md) | 2026-08-18: **FINAL** — live post-S07: schema/compat **26**, MCP **13** (no `trace_loop`), CLI loop live, **3** checklist `[ ]` (C28/C38/C39); stale `trace-mcp -h` (missing regressions). Locked: S08-01→catalog **14**, conflict advisory+`work_conflicts[]`, CONTRIBUTING agent workflow, VERIFY after S09 (schema **27**). Thickened 01–08; spawn/VERIFY policy explicit; no product Go; **next P22-S08-01**. |
| 377 | P22-S08-01 | done | [phases/phase-22-capability-completion/scopes/scope-08-agent-workflow-verify/01-mcp-loop.md](../phases/phase-22-capability-completion/scopes/scope-08-agent-workflow-verify/01-mcp-loop.md) | 2026-08-18: implementer **done**. MCP **`trace_loop`** (`action=next\|apply\|status`) via `internal/mcp/tools_loop.go`; G19 — handlers call `loop.BuildNextPacket`/`ParseApplyEnvelope`/`Apply`/`Status` only (no SQL/domain fork). Catalog **14** (`trace_loop` after `trace_regressions`); `mcp:trace_loop` in `BuiltinMCPCapabilitySpecs`; `cmd/trace-mcp -h` lists all 14. Schema/compat **26** unchanged (no 027+). C38-MCP **true** at code level; checklist C38 **unboxed at S08-02 review**; C39 loop half only (full C39 at S08-05). **PASS:** `go test ./internal/mcp/... -count=1 -run 'TestMCPLoop\|TestToolNamesRegistered'` → ok 0.196s; `go test ./internal/domain/... -count=1 -run TestBuiltinMCPCapabilitySpecs` → ok 0.073s. **Rebuild:** `CGO_ENABLED=0 go build -o bin/trace-mcp ./cmd/trace-mcp` (or `CGO_ENABLED=1` if tree-sitter bindings needed). **Residuals for S08-02:** apply envelope validation parity; DENIED gating review; compiler/retrieval wiring richness vs CLI. **Next `P22-S08-02`**. |
| 378 | P22-S08-02 | done | [phases/phase-22-capability-completion/scopes/scope-08-agent-workflow-verify/02-mcp-loop-review.md](../phases/phase-22-capability-completion/scopes/scope-08-agent-workflow-verify/02-mcp-loop-review.md) | 2026-08-18: independent review **done**. Confidence **high**. **C38 closed** (checklist boxed). **C38-MCP hold:** `trace_loop` (`action=next\|apply\|status`) in `internal/mcp/tools_loop.go` — G19 thin adapter calls `loop.BuildNextPacket`/`ParseApplyEnvelope`/`Apply`/`Status` only (no MCP SQL/domain fork); `assertMCPToolAllowed` before action dispatch; fail-closed on invalid action/missing `task_id`/empty envelope (code paths; no named MCP fail-closed tests — **low** residual). Catalog **14** (`RegisteredToolNames`, `TestToolNamesRegistered`); `mcp:trace_loop` in `BuiltinMCPCapabilitySpecs`; `cmd/trace-mcp -h` lists all 14. Annotations: whole-tool `DestructiveHint=true` (SDK single registration — matches `trace_capability` mixed-action pattern; **FYI** spec wanted per-action read-only hints for next/status). **C38-CLI hold (S03):** `trace test run` + CLI `trace loop`. **C39 loop half only** — full C39 deferred S08-05. Schema **26**; compat **26** PASS; S03 keepers PASS; catalog search/changes/regressions not regressed. **PASS:** `go test ./internal/mcp/... -count=1 -run 'TestMCPLoop\|TestToolNamesRegistered'` → ok 0.156s; `go test ./internal/domain/... -count=1 -run TestBuiltinMCPCapabilitySpecs` → ok 0.038s; `go test ./internal/loop/... -count=1 -run 'TestLoopApply\|TestLoopNextDeliberationSectionPresent'` → ok 0.246s; `CGO_ENABLED=1 go test ./evals/compat/... -count=1 -run TestCompatibilitySecurityChecklist` → ok 1.134s; schema sql count **26**. **Next P22-S08-03**. |
| 379 | P22-S08-03 | done | [phases/phase-22-capability-completion/scopes/scope-08-agent-workflow-verify/03-conflict-redundant.md](../phases/phase-22-capability-completion/scopes/scope-08-agent-workflow-verify/03-conflict-redundant.md) | 2026-08-18: implementer **done**. C28 **true** at code level (checklist **unboxed at S08-04 review**). Advisory-only `domain.DetectWorkConflicts` — path prefix (segment `/` boundary), impact seed intersection (`seedsFromChangePaths` best-effort), same-goal title redundancy (`normalizeTitle`, min **8** runes); active tasks {PENDING, IN_PROGRESS, AWAITING_REVIEW, BLOCKED}; OPEN changes preferred for path union; no locks/blocking. CLI `trace tasks conflicts [--task <id>]` → `{ok,conflicts[]}`; `cli:tasks` gate. Loop next `work_conflicts` section (cap **8**, schema `trace.loop.next.v1` unchanged). Schema/compat **26** (no SQL, no 027+). **PASS:** `go test ./internal/domain/... -count=1 -run 'TestDetectOverlapping\|TestRedundantSimilar\|TestNoConflictWhenTasksDisjoint\|TestNormalizeTitle'` → ok 0.131s; `go test ./internal/loop/... -count=1 -run TestLoopNextIncludesWorkConflicts` → ok 0.055s; `CGO_ENABLED=1 go test ./cmd/trace/... -count=1 -run 'TestTasksConflicts\|TestHelpIncludesTasksConflicts'` → ok 0.091s; schema sql **26**. Files: `internal/domain/conflicts.go`+tests, `internal/loop/next.go`, `cmd/trace/tasks.go`+test, `help.go`. **Residuals for S08-04:** path false positives on short/dir-vs-file prefixes; perf on many open tasks (pairwise scan); S09 E02 routing must reuse detector not fork logic; MCP `include_conflicts` optional (skipped). **Next `P22-S08-04`**. |
| 380 | P22-S08-04 | done | [phases/phase-22-capability-completion/scopes/scope-08-agent-workflow-verify/04-conflict-redundant-review.md](../phases/phase-22-capability-completion/scopes/scope-08-agent-workflow-verify/04-conflict-redundant-review.md) | 2026-08-18: independent review **done**. Confidence **high**. **C28 closed** (checklist boxed). **C28:** advisory-only `domain.DetectWorkConflicts` in `internal/domain/conflicts.go` — path prefix overlap (`/` segment boundary via `pathsOverlap`), impact seed intersection (`seedsFromChangePaths` best-effort), same-goal title redundancy (`normalizeTitle`, min **8** runes); active tasks {PENDING, IN_PROGRESS, AWAITING_REVIEW, BLOCKED}; OPEN changes preferred for path union; stable sort + optional limit. **No blocking:** grep — zero lock table / transition gate / auto-DENIED on overlap; CLI+loop are read-only surfaces. CLI `trace tasks conflicts [--task <id>]` → `{ok,conflicts[]}`; documented in `help.go`. Loop next `work_conflicts` section cap **8** (`buildWorkConflictsSection`); empty `items` when none (section present). Schema/compat **26** (26 sql; no 027+). MCP `include_conflicts` optional — not implemented (CLI required only). **PASS:** domain/loop/cmd named tests; `TestCompatibilitySecurityChecklist`; S03 keepers `TestLoopApply|TestLoopNextDeliberationSectionPresent`; S08-01 keepers `TestMCPLoop|TestToolNamesRegistered|TestBuiltinMCPCapabilitySpecs`. **FYI (low):** O(n²) pairwise scan on many active tasks; S09 E02 must reuse detector not fork logic. **Next `P22-S08-05`**. |
| 381 | P22-S08-05 | done | [phases/phase-22-capability-completion/scopes/scope-08-agent-workflow-verify/05-workflow-useful.md](../phases/phase-22-capability-completion/scopes/scope-08-agent-workflow-verify/05-workflow-useful.md) | 2026-08-18: implementer **done**. C39 **true** at code level (checklist **unboxed at S08-06 review**). Agent workflow discoverability: `TestHelpIncludesSearchTestVerify` asserts help strings for search, test run, verify, changes, knowledge, install git-hook, loop next/apply/status; keeper `TestHelpIncludesLoopNext` unchanged. `cmd/trace-mcp -h` now prints `RegisteredToolNames()` (catalog **14**, order matches `TestToolNamesRegistered`). CONTRIBUTING **Agent workflow (local)** paragraph (index → loop next → test run → loop apply → search/why; stdio MCP + rebuild + `trace_version`; link `docs/rules/agent-loop-protocol.md`). Schema/compat **26** (no 027+). **PASS:** `GOMODCACHE=$HOME/go/pkg/mod GOPROXY=off CGO_ENABLED=1 go test ./cmd/trace/... ./internal/mcp/... -count=1 -run 'TestHelpIncludesSearchTestVerify\|TestToolNamesRegistered\|TestHelpIncludesLoopNext'` → ok cmd/trace 0.008s ok mcp 0.007s; `TestTraceVersion` smoke → ok. **Rebuild:** `GOMODCACHE=$HOME/go/pkg/mod GOPROXY=off CGO_ENABLED=1 go build -o bin/trace-mcp ./cmd/trace-mcp` (CGO_ENABLED=0 fails here when analyzers dep lacks CGO build tags). **`-h` snippet:** `Tools: trace_why, trace_context, trace_add, trace_link, trace_transition, trace_review, trace_tasks, trace_capability, trace_impact, trace_version, trace_search, trace_changes, trace_regressions, trace_loop.` Files: `cmd/trace/help_test.go`, `cmd/trace-mcp/main.go`, `CONTRIBUTING.md`. **Residuals for S08-06:** box C39 checklist; confirm `-h`=`RegisteredToolNames()` on reviewer machine; S09 adds 15th tool (`trace_agents`) — do not claim final catalog until S09-08; README one-liner skipped (CONTRIBUTING sufficient); CGO_ENABLED=0 vs 1 rebuild env variance. **Next `P22-S08-06`**. |
| 382 | P22-S08-06 | done | [phases/phase-22-capability-completion/scopes/scope-08-agent-workflow-verify/06-workflow-useful-review.md](../phases/phase-22-capability-completion/scopes/scope-08-agent-workflow-verify/06-workflow-useful-review.md) | 2026-08-18: independent review **done**. Confidence **high**. **C39 closed** (checklist boxed). **C38 hold** (checklist still `[x]`). **C39:** `TestHelpIncludesSearchTestVerify` PASS — help strings for search, test run, verify, changes, knowledge, install git-hook, loop next/apply/status; keeper `TestHelpIncludesLoopNext` PASS. CONTRIBUTING **Agent workflow (local)** § accurate (index → loop next → test run → loop apply → search/why; stdio MCP + rebuild + `trace_version`; link `docs/rules/agent-loop-protocol.md`). MCP catalog **14**: `TestToolNamesRegistered` order matches `RegisteredToolNames()`; live `./bin/trace-mcp -h` → `Tools: trace_why, trace_context, trace_add, trace_link, trace_transition, trace_review, trace_tasks, trace_capability, trace_impact, trace_version, trace_search, trace_changes, trace_regressions, trace_loop.` Rebuild evidence: S08-05 Notes + `bin/trace-mcp` mtime **2026-08-18 22:24** (+0330). **C38 hold:** checklist `[x]` from S08-02; `TestMCPLoopNext|Apply|Status` PASS (G19 thin adapter); CLI test/verify halves from S03-04/S03-08 keepers cited on board. **Landmines:** no hosted MCP/daemon docs in S08-05 delta; no `trace_agents` claim (S09); schema **26** sql (no 027+). **PASS:** `CGO_ENABLED=1 go test ./cmd/trace/... ./internal/mcp/... -count=1 -run 'TestHelpIncludesSearchTestVerify|TestToolNamesRegistered|TestHelpIncludesLoopNext|TestMCPLoop'` → ok cmd/trace 0.008s ok mcp 0.147s. **FYI (low):** S09 adds 15th tool — catalog not final until S09-08; CGO_ENABLED rebuild env variance (S08-05). **Next `P22-S09-00`**. |
| 383 | P22-S09-00 | done | [phases/phase-22-capability-completion/scopes/scope-09-harness-agent-catalog/00-PLANNER.md](../phases/phase-22-capability-completion/scopes/scope-09-harness-agent-catalog/00-PLANNER.md) | 2026-08-18: **FINAL** — live post-S08: schema/compat **26**, MCP **14**, no `internal/agents/`, no mig **027**, `trace/agents/README.md` stub only; AGENT kind rejected in `TestUpsertCapabilityGetAndReject`. Locked D-22-25…30: recommend-only, mig **027** `harness_agents`+requirements, bundled `default.json`, `harness_recommendations[]` in loop next, MCP catalog **15** (`trace_agents`), VERIFY (S08-07) after S09. Thickened **01–08** with live baseline, DDL, routing rules, named tests, spawn policy, E01–E04 mapping; residual risks per row. No product Go. **Next `P22-S09-01`**. |
| 384 | P22-S09-01 | done | [phases/phase-22-capability-completion/scopes/scope-09-harness-agent-catalog/01-agent-schema-routing.md](../phases/phase-22-capability-completion/scopes/scope-09-harness-agent-catalog/01-agent-schema-routing.md) | 2026-08-18: **E03+E02** — mig **027** `harness_agents`+requirements; compat ceiling **27**; `CapabilityKindAgent`; store/domain CRUD; `internal/agents/routing.go` deterministic `RecommendAgents` (phase+keyword, cap 4, empty catalog→[]); seed export/import `harness_agents[]`+`requirements[]`. **Tests:** `go test ./internal/agents/... ./internal/domain/... ./internal/store/... -count=1 -run 'TestHarnessAgent|TestCapabilityKindAgent|TestRecommend|TestRoutingDeterministic'` **PASS**; `CGO_ENABLED=1 go test ./evals/compat/... -count=1 -run TestCompatibilitySecurityChecklist` **PASS**; 27 SQL files. No CLI/MCP/install/loop. Residual: JSON fail-closed, seed merge semantics → S09-02. **Next P22-S09-02**. |
| 385 | P22-S09-02 | done | [phases/phase-22-capability-completion/scopes/scope-09-harness-agent-catalog/02-agent-schema-routing-review.md](../phases/phase-22-capability-completion/scopes/scope-09-harness-agent-catalog/02-agent-schema-routing-review.md) | 2026-08-18: independent review **done**. Confidence **high**. **E03 foundation closed** (schema mig **027**, store/domain CRUD, seed export/import round-trip); bundled `default.json`+install deferred **S09-03**. **E02 routing library closed** at library layer (`RecommendAgents` deterministic phase+keyword rules, cap **4**, empty catalog→`[]`); loop `harness_recommendations[]`/CLI/MCP deferred **S09-05/07**. **E03:** `027_harness_agents.sql` only (27 SQL files); DDL matches lock (slug UNIQUE, requirements UNIQUE agent+slug, nullable `external_url`); `TestHarnessAgentCatalogMigrate027`+`TestHarnessAgentJSONValidation` (JSON fail-closed via `requireJSONArray`); `TestHarnessAgentSeedExportRoundTrip` PASS. **E02:** `TestRecommendAgentForPhaseCritique`, `TestRecommendPerformanceReviewerForPerfTask`, `TestRoutingDeterministic`, `TestRecommendEmptyCatalog`, `TestRecommendMissingCapabilities` PASS; grep — no `Task(`, subprocess runner, HTTP registry fetch. **AGENT:** `CapabilityKindAgent` store+domain; `TestCapabilityKindAgent` PASS; inline-fix — `TestUpsertCapabilityGetAndReject` now asserts AGENT succeeds; unknown kinds still fail. **Boundaries:** no CLI `trace agents`, no MCP `trace_agents`, no loop packet, no `default.json`; MCP catalog still **14** (`TestBuiltinMCPCapabilitySpecs`). **Compat:** ceiling **27** PASS. **P21 keepers:** `TestSeedExportRoundTrip`, `TestSeedExportOmitsDeniedSurfaces` PASS. **PASS:** `go test ./internal/agents/... ./internal/domain/... ./internal/store/... -count=1 -run 'TestHarnessAgent|TestCapabilityKindAgent|TestRecommend|TestRoutingDeterministic'` → ok; `CGO_ENABLED=1 go test ./evals/compat/... -count=1 -run TestCompatibilitySecurityChecklist` → ok; `go test ./cmd/trace/... -count=1 -run 'TestSeedExportRoundTrip|TestSeedExportOmitsDeniedSurfaces'` → ok. **FYI (low):** no dedicated cap-4 overflow test (code enforces `defaultMaxResults=4`); `requireJSONArray` error strings still say "upsert reflection" (pre-existing helper); routing uses locked hardcoded rules not DB `deliberation_phases` columns (by design S09-01). **No spawn.** **Next `P22-S09-03`**. |
| 386 | P22-S09-03 | done | [phases/phase-22-capability-completion/scopes/scope-09-harness-agent-catalog/03-bundled-defaults-install.md](../phases/phase-22-capability-completion/scopes/scope-09-harness-agent-catalog/03-bundled-defaults-install.md) | 2026-08-18: **E03+E04** — committed `trace/agents/default.json` (schema v1, **6** profiles, registry_version **2026-08-18**); `internal/agents/builtin.go` loader+validation; `internal/install/agents.go` `InstallAgentDefaults` (idempotent upsert by slug, SKILL/MCP UNKNOWN stubs, `hook:harness:subagent` HOOK AVAILABLE when `.cursor/` / `$HOME/.cursor/rules` / `TRACE_HARNESS_SUBAGENT=1` else UNKNOWN); CLI `trace install agents` + `trace init --with-agent-defaults`; help updated; README E04 extension prose. **Tests:** `TestInstallAgentsSeedsDefaults`, `TestDefaultCatalogValidJSON`, `TestBundledProfilesIncludeRequirements`, `TestInstallAgentsIdempotent`, `TestSubagentHookDeclaredOnInstall` PASS; compat **27** PASS. No CLI `trace agents list`/MCP `trace_agents` (S09-07). Residual: module-root path for installed binary → S09-04. **Next `P22-S09-04`**. |
| 387 | P22-S09-04 | done | [phases/phase-22-capability-completion/scopes/scope-09-harness-agent-catalog/04-bundled-defaults-install-review.md](../phases/phase-22-capability-completion/scopes/scope-09-harness-agent-catalog/04-bundled-defaults-install-review.md) | 2026-08-18: independent review **done**. Confidence **high**. **E03 closed at install layer** — `trace/agents/default.json` schema v1, **6** profiles (`agent:*`), embed via `trace/agents/catalog_embed.go`; `TestDefaultCatalogValidJSON`, `TestBundledProfilesIncludeRequirements`, `TestEmbeddedDefaultCatalogMatchesFile` PASS. **E04 closed at install layer** — `registry_source`/`registry_version`/`external_url` populated on upsert; README future-host prose + no-network lock; no `http.Get`/daemon. **Install:** inline fix — CLI used cwd `go.mod` path (failed outside Trace repo); now `LoadEmbeddedDefaultCatalog()` default, `CatalogPath` override for tests; verified `trace install agents` from foreign `go.mod` project; `trace init --with-agent-defaults` same loader; help mentions both. **hook:harness:subagent** — UNKNOWN without harness signals, AVAILABLE with `.cursor`/env; no subprocess/Task. Schema **27**, MCP **14**, S09-01 routing tests PASS. No Na/Nb spawned. **Next P22-S09-05**. |
| 388 | P22-S09-05 | done | [phases/phase-22-capability-completion/scopes/scope-09-harness-agent-catalog/05-routing-loop-packet.md](../phases/phase-22-capability-completion/scopes/scope-09-harness-agent-catalog/05-routing-loop-packet.md) | 2026-08-18: implementer **done**. **E01+E02** — additive `harness_recommendations` on `NextPacket` (`trace.loop.next.v1` unchanged); `buildHarnessRecommendationsSection` calls `agents.RecommendAgents` + `loadHarnessCapsForRouting` (reuses S08 `DetectWorkConflicts` via existing `work_conflicts` section — no duplicate); subagent gate maps `hook:harness:subagent`→`harness:subagent` for routing; `use_subagent`+`prompt_stub` when AVAILABLE. **C09:** phase+keyword routing visible in loop next (CRITIQUE→code-reviewer, VERIFY+perf→performance-reviewer). **C28:** work_conflicts unchanged (S08). **C39:** recommend-only — no Task/subprocess invocation (D-22-25). **Tests:** `TestLoopNextIncludesHarnessRecommendations`, `TestRecommendSubagentWhenAvailable`, `TestRecommendSubagentHonestWhenUnavailable`, `TestRecommendPerformanceReviewerForPerfTask`, `TestNoRecommendationWhenCatalogEmpty` PASS; `TestLoopNextDeliberationSectionPresent` keeper green; full `./internal/loop/...` PASS. No CLI `trace agents` / MCP `trace_agents` (S09-07). Optional `--agents`/markdown deferred. **Next `P22-S09-06`**. |
| 389 | P22-S09-06 | done | [phases/phase-22-capability-completion/scopes/scope-09-harness-agent-catalog/06-routing-loop-packet-review.md](../phases/phase-22-capability-completion/scopes/scope-09-harness-agent-catalog/06-routing-loop-packet-review.md) | 2026-08-18: independent review **done**. Confidence **high**. **E01 closed at loop layer** — `TestRecommendSubagentWhenAvailable`+`TestRecommendSubagentHonestWhenUnavailable` PASS (`use_subagent`+prompt_stub when `hook:harness:subagent` AVAILABLE; honest false/empty stub when UNKNOWN); prompt mentions fresh session/not implementer; grep `internal/loop/next.go`+`internal/agents/routing.go` — no Task/subprocess spawn (D-22-25). **E02 closed at loop layer** — `TestLoopNextIncludesHarnessRecommendations`, `TestRecommendPerformanceReviewerForPerfTask` PASS; CRITIQUE→code-reviewer/nested-reviewer via `TestRecommendAgentForPhaseCritique` (library)+packet; VERIFY+perf keywords→performance-reviewer; INVESTIGATE/ORIENT→explore in `collectCandidates` (code, no dedicated loop test); cap **4** (`defaultMaxResults`); empty catalog→freshness unknown (`TestNoRecommendationWhenCatalogEmpty`); `missing_capabilities[]` via `TestRecommendMissingCapabilities` (library, loop passthrough). **C09/C28/C39 strengthening (note only, checklist already [x]):** loop next surfaces reviewer profile+subagent hint on CRITIQUE (`TestLoopNextIncludesHarnessRecommendations`+subagent tests); specialized routing vs generic fallback (`agent:generalPurpose` score 10); harness recommendations on normal `BuildNextPacket` path additive (`trace.loop.next.v1` unchanged). **Keepers:** `TestLoopApply` PASS (`./internal/loop`); `TestLoopNextDeliberationSectionPresent` PASS (`CGO_ENABLED=1 ./cmd/trace`). **Boundaries:** schema **27** (27 SQL files, compat PASS); MCP catalog **14** (`TestToolNamesRegistered`). **FYI (low):** scope re-run cmd lists deliberation keeper under `./internal/loop/...` but test lives in `cmd/trace/loop_test.go`; no loop-layer test for INVESTIGATE→explore; E02 CLI/MCP query surface deferred **S09-07**. **No spawn.** **Next `P22-S09-07`**. |
| 390 | P22-S09-07 | done | [phases/phase-22-capability-completion/scopes/scope-09-harness-agent-catalog/07-cli-mcp-agents.md](../phases/phase-22-capability-completion/scopes/scope-09-harness-agent-catalog/07-cli-mcp-agents.md) | 2026-08-18: implementer **done**. **E02+E04** — CLI `trace agents list|describe|recommend` + MCP **`trace_agents`** (`list|describe|recommend`); G19 thin adapters call `internal/agents` catalog + `loop.RecommendHarness` (same `RecommendAgents` path as loop next). Catalog **15** (`trace_agents` after `trace_loop`); `mcp:trace_agents` + `cli:agents` specs; help updated. **Tests:** `TestCLIAgentsList`, `TestCLIAgentsDescribe`, `TestCLIAgentsRecommend`, `TestMCPAgentsList`, `TestMCPAgentsRecommend`, `TestToolNamesRegistered`, `TestBuiltinMCPCapabilitySpecs`, `TestHelpIncludesAgents` PASS; compat **27** PASS. **Rebuild:** `CGO_ENABLED=0 go build -o bin/trace-mcp ./cmd/trace-mcp`. **Next `P22-S09-08`**. |
| 391 | P22-S09-08 | done | [phases/phase-22-capability-completion/scopes/scope-09-harness-agent-catalog/08-cli-mcp-agents-review.md](../phases/phase-22-capability-completion/scopes/scope-09-harness-agent-catalog/08-cli-mcp-agents-review.md) | 2026-08-18: independent review **done**. Confidence **high**. **S09 scope complete (E01–E04 closed).** **E01** — `TestRecommendSubagentWhenAvailable`, `TestRecommendSubagentHonestWhenUnavailable`, `TestLoopNextIncludesHarnessRecommendations` PASS (loop `harness_recommendations[]` + fresh-subagent stub when `hook:harness:subagent` AVAILABLE). **E02** — `TestCLIAgentsList|Describe|Recommend`, `TestMCPAgentsList|Recommend`, `TestRecommendPerformanceReviewerForPerfTask` + routing keepers PASS; CLI/MCP share `loop.RecommendHarness` (parity for `--phase CRITIQUE`). **E03** — `trace/agents/default.json`, `TestInstallAgentsSeedsDefaults`, mig **027** (27 SQL files). **E04** — `trace/agents/README.md` extension prose + `registry_*`/`external_url` columns; grep — no `http.Get`/daemon in agents/install paths. **W-40:** catalog **15** (`TestToolNamesRegistered`, `trace_agents` last); `mcp:trace_agents` + `cli:agents` in builtin specs; `assertMCPToolAllowed` + ReadOnly MCP annotations. **Hard boundaries:** no Task/spawn/subprocess agent runner in S09 surfaces (`cmd/trace/agents.go`, `internal/mcp/tools_agents.go`, `internal/agents`, `internal/loop` recommend path). Compat **27** PASS. **FYI (low):** committed `bin/trace-mcp` may predate S09-07 — rebuild `CGO_ENABLED=0 go build -o bin/trace-mcp ./cmd/trace-mcp` for live `-h` tail. **No spawn.** **Next `P22-S08-07` VERIFY** (board order). |
| 392 | P22-S08-07 | done | [phases/phase-22-capability-completion/scopes/scope-08-agent-workflow-verify/07-verify.md](../phases/phase-22-capability-completion/scopes/scope-08-agent-workflow-verify/07-verify.md) | 2026-08-18: VERIFY **PASS**. **Precond:** S09-00…S09-08 `done`. **Checklist:** 141/141 `[x]`, 0 open. **Matrix:** C01–C43 + E01–E04 evidenced (see VERIFY-NOTES). **Command floor:** all 13 blocks PASS (`experiments/runs/2026-08-18-p22-s08-07-verify/evidence/`). **Schema/compat:** 27 SQL, ceiling 27. **CLI smoke:** install detect git-hook id, query help, install agents + recommend CRITIQUE (recommend-only), seed export omits index + 6 harness_agents. **FYI:** `bin/trace-mcp -h` shows 14 tools (stale binary); `TestToolNamesRegistered` confirms 15 incl. `trace_agents`. Refreshed `trace/graph.json` (6 harness_agents). **DR-HANDOFF:** OPEN — **S08-08** closes. **No spawn.** **Next `P22-S08-08`**. |
| 393 | P22-S08-08 | done | [phases/phase-22-capability-completion/scopes/scope-08-agent-workflow-verify/08-verify-review.md](../phases/phase-22-capability-completion/scopes/scope-08-agent-workflow-verify/08-verify-review.md) | 2026-08-18: independent review **APPROVE — confidence high**. Fresh ≠ S08-07. Command floor **PASS** (compat **27**; 27 SQL / no 028+; S01–S09 + P21 keepers). Checklist **141/141 `[x]`**. C01–C43 + E01–E04 PASS. Live `./bin/trace-mcp -h` **15** tools incl. `trace_agents` (rebuilt CGO1 this session). S01–S08 reviewers closed in-phase (S03-06a/b, S05-02a/b, S06-02a/b); no Phase 23. **DR-HANDOFF CLOSED** — **`no successor`**. Phase 22 **complete**. Evidence: `experiments/runs/2026-08-18-p22-s08-08-review/` + [`REVIEW-NOTES.md`](../phases/phase-22-capability-completion/scopes/scope-08-agent-workflow-verify/REVIEW-NOTES.md). P21 historical `no successor` intact. **Next: none**. |

---
