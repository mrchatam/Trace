Index: [`docs/TODO.md`](../TODO.md)

## Phase 19 — Loop gap detection

Human-promoted forward follow-on after Phase 18 dogfood and taskboard D42. Phase 18 historical close remains true at time-of-close; Phase 19 is a new queue. Goal: add a **stdout-first**, harness-agnostic loop surface so fresh agents can repeatedly consume Trace context, identify gaps, record discoveries/plan changes/tasks, and stop when the loop saturates.

| Order | ID | Status | Prompt | Notes |
|------:|----|--------|--------|-------|
| 261 | P19-00 | done | [phases/phase-19-loop-gap-detection/00-PHASE-PLANNER.md](../phases/phase-19-loop-gap-detection/00-PHASE-PLANNER.md) | 2026-08-18: planner executed against live repo. Confirmed reusable MVP seams in `cmd/trace/{tasks,why,context,plan,impact}.go` plus `cmd/trace/root.go`; Phase 19 should add a loop-oriented envelope, not a second planning substrate. Tightened Phase 19 README + `AGENTS.md`; thickened upcoming S01/S02/S03 planner/implement/review prompts with explicit packet sections, freshness enum (`fresh|dirty|stale|unknown`), structured fail-closed apply/status semantics, and VERIFY mini-eval / DR-HANDOFF expectations. No product Go. No new rows spawned. **Next runnable `P19-S01-00`**. |
| 262 | P19-S01-00 | done | [phases/phase-19-loop-gap-detection/scopes/scope-01-loop-next-packet/00-PLANNER.md](../phases/phase-19-loop-gap-detection/scopes/scope-01-loop-next-packet/00-PLANNER.md) | 2026-08-18: scope planner executed against live CLI seams in `cmd/trace/{tasks,why,context,plan,impact,root}.go`. Locked S01 to a narrow `trace loop next --task <id>` MVP that derives `goal_id` from the seed task, reuses `tasks` / `why` / `context --depth 1 --include-why` / `plan show --goal` / `impact walk` semantics where practical, and keeps the packet bounded plus stdout-first. Thickened upcoming implement/review prompts with explicit section-level freshness (`fresh|dirty|stale|unknown`), packet presence requirements, likely touch points, and fail-closed behavior for missing task, missing `goal_id`, and missing goal-plan context. No product Go. No new rows spawned. **Next runnable `P19-S01-01`**. |
| 263 | P19-S01-01 | done | [phases/phase-19-loop-gap-detection/scopes/scope-01-loop-next-packet/01-loop-next-packet.md](../phases/phase-19-loop-gap-detection/scopes/scope-01-loop-next-packet/01-loop-next-packet.md) | 2026-08-18: implemented `trace loop next --task <id>` as a thin CLI adapter plus `internal/loop` packet builder that composes task, plan, why, context, and related impact-walk neighborhood data into one bounded JSON packet with explicit section freshness. Fail-closed behavior added for missing task, missing `goal_id`, and missing goal-plan context; `loop_hints` now reports explicit absence when iteration metadata is unavailable. Registered `cli:loop` in builtin capability gating, updated root/help wiring, and added focused CLI coverage for packet shape/help plus the fail-closed paths. Verification: `go test ./cmd/trace -run 'TestLoopNext|TestHelpIncludesLoopNext'` and `go test ./cmd/trace` both passed. No rows spawned. **Next runnable `P19-S01-02`**. |
| 264 | P19-S01-02 | done | [phases/phase-19-loop-gap-detection/scopes/scope-01-loop-next-packet/02-scope-review.md](../phases/phase-19-loop-gap-detection/scopes/scope-01-loop-next-packet/02-scope-review.md) | 2026-08-18: independent review passed with **high confidence**. Verified narrow CLI contract (`trace loop next --task <id>`), root/help wiring, and fail-closed behavior for missing task, missing `goal_id`, and missing goal-plan context. Confirmed packet remains bounded and provenance-preserving: seed/tasks/plan/why/context/related/loop_hints sections are explicit with freshness or availability markers and no silent omission; related neighborhood is bounded via impact-walk seeds from context (no full-graph dump). Re-verified checks: `go test ./cmd/trace -run 'TestLoopNext|TestHelpIncludesLoopNext'` and `go test ./cmd/trace` (pass). No blocker/high findings. No rows spawned. **Next runnable `P19-S02-00`**. |
| 265 | P19-S02-00 | done | [phases/phase-19-loop-gap-detection/scopes/scope-02-loop-apply-saturation/00-PLANNER.md](../phases/phase-19-loop-gap-detection/scopes/scope-02-loop-apply-saturation/00-PLANNER.md) | 2026-08-18: scope planner executed against live seams (`cmd/trace/loop.go`, `cmd/trace/plan.go`, `internal/store/{entities,links}.go`). Locked `trace.loop.apply.v1` envelope (required `schema_version`, `apply_id`, `seed.task_id`, `seed.goal_id`, `writes` object) with stdin-or-file ingestion and fail-closed validation. Locked replay-safe semantics to caller-supplied stable IDs + upsert-by-id entities + `InsertLinkOrIgnore` link dedupe, and required append-only loop-step event evidence for status computation. Locked `trace.loop.status.v1` output fields and saturation reasons (`tasks_and_plan_unchanged`, `max_iterations_reached`, `insufficient_history`) with explicit no-history behavior. Thickened upcoming `P19-S02-01`/`P19-S02-02` prompts and verification expectations. No product Go. No rows spawned. **Next runnable `P19-S02-01`**. |
| 266 | P19-S02-01 | done | [phases/phase-19-loop-gap-detection/scopes/scope-02-loop-apply-saturation/01-loop-apply-saturation.md](../phases/phase-19-loop-gap-detection/scopes/scope-02-loop-apply-saturation/01-loop-apply-saturation.md) | 2026-08-18: implemented `trace loop apply` (`--in <path>` or stdin) and `trace loop status` with fail-closed `trace.loop.apply.v1` validation (required `schema_version`, UUID `apply_id`, UUID seed ids, and stable per-item ids/titles where required). Added `internal/loop/apply.go` writeback + status engine that reuses domain/planner seams (`ImportSeedDiscovery/PlanChange/Task`, `ImportSeedLink`, optional `planner.ApplyDiscoveryReplan`), persists one append-only `loop.step.applied` summary event on successful apply, computes status from persisted step evidence only, and enforces replay no-op by `apply_id`/seed. Help text expanded for `loop apply`/`loop status`; CLI coverage added for malformed input fail-closed paths, replay/idempotency, no-history status, zero-delta saturation, and max-iteration saturation reason. Verification: `go test ./cmd/trace -run Loop`, `go test ./cmd/trace`, and manual CLI proof (`loop apply` → `loop status`) showing `last_apply_id` propagation and `tasks_and_plan_unchanged` saturation. No rows spawned. **Next runnable `P19-S02-02`**. |
| 267 | P19-S02-02 | done | [phases/phase-19-loop-gap-detection/scopes/scope-02-loop-apply-saturation/02-scope-review.md](../phases/phase-19-loop-gap-detection/scopes/scope-02-loop-apply-saturation/02-scope-review.md) | 2026-08-18: independent fresh-session review completed with **high confidence** and no blocker/high findings. Verified repo evidence in `cmd/trace/loop.go` + `internal/loop/apply.go`: fail-closed `trace.loop.apply.v1` contract enforcement (`schema_version`, `apply_id`, seed IDs, stable item IDs), replay no-op by persisted loop-step `apply_id`/seed, append-only `loop.step.applied` event write only on success, status computed from persisted step events (not process memory), and saturation reasons constrained to `tasks_and_plan_unchanged` / `max_iterations_reached` / `insufficient_history`. Re-ran locked checks: `go test ./cmd/trace -run 'TestLoopApplyMalformedInputFailsClosed|TestLoopApplyReplayAndStatusFlow|TestLoopStatusInsufficientHistory|TestLoopStatusSaturatedByZeroDeltaAndMaxIteration|TestHelpIncludesLoopNext'` (pass). Re-ran CLI proof for apply/status contract with both input forms: file apply (`--in`), replay same packet, stdin apply, no-history status, zero-delta saturation, and max-iteration saturation (`CLI_PROOF_OK`) (pass). No rows spawned. **Next runnable `P19-S03-00`**. |
| 268 | P19-S03-00 | done | [phases/phase-19-loop-gap-detection/scopes/scope-03-phase-verify/00-PLANNER.md](../phases/phase-19-loop-gap-detection/scopes/scope-03-phase-verify/00-PLANNER.md) | 2026-08-18: planner row executed against live loop implementation (`cmd/trace/loop.go`, `internal/loop/{next,apply}.go`, `cmd/trace/loop_test.go`). Locked S03 verify bar to ordinary CLI proof for `loop next/apply/status`, schema/version expectations (`trace.loop.next.v1`, `trace.loop.apply.v1`, `trace.loop.status.v1`), status-reason constraints, fail-closed malformed apply evidence, and replay no-op evidence. Thickened `P19-S03-01` mini-eval requirements to require at least one forward packet-driven write plus explicit residual-risk wording when fixture-scale is used. Locked `P19-S03-02` handoff-close protocol and updated `DR-HANDOFF.md` so S03-02 must make explicit successor decision and may not leave `TBD` on close. No product Go edits. No new rows spawned. **Next runnable `P19-S03-01`**. |
| 269 | P19-S03-01 | done | [phases/phase-19-loop-gap-detection/scopes/scope-03-phase-verify/01-verify.md](../phases/phase-19-loop-gap-detection/scopes/scope-03-phase-verify/01-verify.md) | 2026-08-18: verify row executed via ordinary CLI (`go run ./cmd/trace -C <fixture> ...`) with archived evidence under `experiments/runs/2026-08-18-p19-s03-01-verify/evidence/`. Locked test floor passed: `go test ./cmd/trace -run 'TestLoopNextPacketShape|TestLoopApplyMalformedInputFailsClosed|TestLoopApplyReplayAndStatusFlow|TestLoopStatusInsufficientHistory|TestLoopStatusSaturatedByZeroDeltaAndMaxIteration|TestHelpIncludesLoopNext'`. Captured required artifacts: `loop next` packet (`07-loop-next.json`) with `trace.loop.next.v1` and explicit `seed/tasks/plan/why/context/related/loop_hints`; `loop apply` structured write (`11-apply-first.json`) plus replay no-op (`12-apply-replay.json` with `replay:true` + stable counts); `loop status` unsaturated no-history (`06-status-insufficient-history.json`, `reason=insufficient_history`) and saturated paths (`16-status-saturated-zero-delta.json` reason `tasks_and_plan_unchanged`; `19-status-saturated-max-iteration.json` reason `max_iterations_reached`). Fail-closed malformed apply proven (`09-apply-malformed-exit.txt` exit 1, stderr `apply_id must be UUID`) with no partial writes. Mini-eval bar satisfied on fixture-scale seed/goal/plan context: packet-driven forward write created one discovery/plan-change/spawned-task via `10-apply-valid.json`, reflected by `20-tasks-after-apply.json`; residual risk: this proof is fixture-scale and does not yet validate variance from a live continuation of the D42 repo state. No rows spawned. **Next runnable `P19-S03-02`**. |
| 270 | P19-S03-02 | done | [phases/phase-19-loop-gap-detection/scopes/scope-03-phase-verify/02-scope-review.md](../phases/phase-19-loop-gap-detection/scopes/scope-03-phase-verify/02-scope-review.md) | 2026-08-18: independent fresh-session review completed with **high confidence** and no blocker/high findings. Re-ran locked verify floor: `go test ./cmd/trace -run 'TestLoopNextPacketShape|TestLoopApplyMalformedInputFailsClosed|TestLoopApplyReplayAndStatusFlow|TestLoopStatusInsufficientHistory|TestLoopStatusSaturatedByZeroDeltaAndMaxIteration|TestHelpIncludesLoopNext'` (pass). Re-reviewed archived S03-01 evidence under `experiments/runs/2026-08-18-p19-s03-01-verify/evidence/`: schema anchors confirmed (`trace.loop.next.v1`, `trace.loop.apply.v1`, `trace.loop.status.v1`), status reasons constrained to `insufficient_history` / `tasks_and_plan_unchanged` / `max_iterations_reached`, malformed apply rejection evidenced (`09-apply-malformed-exit.txt` + stderr UUID error), replay no-op evidenced (`12-apply-replay.json` + live replay check from `10-apply-valid.json`), and mini-eval forward write evidenced (`10-apply-valid.json` -> `20-tasks-after-apply.json`). Live ordinary CLI spot-checks (`go run ./cmd/trace -C <fixture> loop next|apply|status`) remained harness-agnostic and consistent with persisted-state reasoning. `DR-HANDOFF.md` is now explicitly **CLOSED** with successor decision **`no successor`**. No rows spawned. **Next runnable: none (Phase 19 queue complete).** |

---
